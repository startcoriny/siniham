# Checklist - 화면 우선 MVP 구현

기준 문서. [docs/specs/screen-design.md](docs/specs/screen-design.md) (11장 "화면 우선 개발 순서" 1~10단계를 기본 골격으로 삼되, 아래 진행 순서를 따른다)

방향 전환. Docker/WSL2 문제로 DB 연결이 막혀서, 실제 API 없이 목업 데이터 + localStorage로 1~9단계(전체 화면)를 먼저 완성하고 10단계에서 실제 API로 교체하기로 결정.

리스크 완화 장치. `shared/types/*`에 실제 API와 주고받을 타입을 미리 정의하고 목업 데이터가 그 타입을 그대로 따르게 한다. 나중에 로컬 상태를 API 호출로 바꿀 때 화면 컴포넌트는 거의 건드리지 않는 것이 목표.

## 진행 순서 (캐릭터 에셋 우선순위 재조정, 2026-08-01)

햄스터 스프라이트가 아직 골든 3포즈만 있어서, 캐릭터 의존도가 낮은 화면부터 먼저 만들기로 결정.

1. 3단계 - 공통 게임 레이아웃 (캐릭터 없음)
2. 8단계 - 상점 화면 (캐릭터 없음)
3. 설정 및 계정 화면 - 원래 10단계 목록에 없던 화면, 이번에 추가 (캐릭터 없음)
4. 9단계 중 미션 탭 (캐릭터 없음)
5. 7단계 - 정원 화면 (캐릭터 의존 낮음, 자리표시자로 진행)
6. 9단계 중 행동 도감 탭 (캐릭터 의존 낮음 - 미발견 상태는 스펙상 실루엣이라 원래 실제 그림 불필요)

**보류.** 온보딩(외형 선택 자체가 캐릭터 중심), 케이지 화면의 캐릭터 상호작용/애니메이션(4, 5, 6단계), 시작 화면의 캐릭터 연출. 골든 12포즈 + 나머지 색상이 어느 정도 채워지면 이어서 진행.

## 이전에 완료한 작업

- [x] npm workspace 스캐폴딩 (client / server / shared) - 커밋 e248503
- [x] 백엔드 인증 API 코드 (signup / login / logout / me) - DB 미연결로 대기 중
- [x] 1단계 디자인 시스템 (컬러 토큰, PixelButton, PixelCard, Modal, BottomSheet, LoadingHamster, Toast, StatusBar, BottomTabBar, SideMenu) - 커밋 ef77cc3
- [x] 캐릭터 타입/에셋 인프라 (`shared/types/hamster.ts`, `hamsterAssets.ts`, `HamsterSprite` 자동 폴백) - 커밋 b9a4873
- [x] 닉네임 + 비밀번호 목업 인증 (`tester`/`1234`), `AuthContext`, `ProtectedRoute`, AuthPage 재작업 - 커밋 9cb0a0d

미해결로 남겨둔 것 (2단계 나머지, 보류 그룹과 함께 나중에). StartPage 분리 여부, 로그인/회원가입 화면 분리 여부, 오류 상태 세분화, 온보딩 1/2단계, 로그인 성공 시 `/home` vs `/onboarding` 분기. 서버는 여전히 email 기준(10단계에서 닉네임으로 정정 필요).

---

## 1. 3단계 - 공통 게임 레이아웃

- [x] `GameStateContext` 최초 도입 - 재화만 우선(기본 100, localStorage 유지), 이후 단계에서 필드 확장
- [x] `GameShell` 레이아웃 - 상단 정보 영역(닉네임/재화/설정 버튼). 알림은 실제 알림 시스템이 생기는 단계에서 추가
- [x] PC 좌측 메뉴 (`md:` 이상, 1단계 `SideMenu` 재사용) / 모바일 하단 탭 (`md:` 미만, `BottomTabBar` 재사용) - Tailwind `md`(768px) 기준으로 분기, 정확한 기준 px는 설계서에 없어 임의 선택
- [x] React Router 중첩 라우트로 탭 전환 (`/home/cage`, `/home/garden`, `/home/shop`, `/home/collection`, `/home/settings`) - 설정은 탭이 아니라 상단 버튼으로 접근(설계서 4.1 다이어그램 기준)
- [x] 각 탭은 순서대로 채워질 때까지 임시 자리표시자 화면
- [x] 버그 수정. 설정 화면처럼 4탭에 없는 경로에서 좌측메뉴/하단탭이 "케이지"를 잘못 활성 표시하던 것 수정

검증. PC(1280px)/모바일(390px) 각각 케이지/정원/상점/설정 탭 전환 스크린샷, typecheck·build 통과

## 2. 8단계 - 상점 화면

- [x] `shared/types/cage.ts` - ItemId, ItemMasterInfo, CageItem 타입. ITEM_MASTERS 카탈로그(5종), STARTER_ITEM_IDS
- [x] ShopItemCard (쳇바퀴 100 / 터널 150, 먹이통·물통·집은 기본 지급이라 상점 목록에서 제외)
- [x] PurchaseModal (구매 확인 -> 재화 확인 -> 완료)
- [x] 재화 부족 안내 상태 ("재화가 조금 부족해요..." + 구매 버튼 비활성화)
- [x] `GameStateContext`에 `ownedItemIds`, `purchaseItem()` 추가 (재화는 1번 작업에서 이미 있음)

검증. 쳇바퀴(재화 100 정확히 소진) 구매 성공 -> "보유중" 표시, 터널(150) 구매 시도 -> 재화 부족 안내, Playwright 스크린샷 확인. typecheck·build 통과.

## 3. 설정 및 계정 화면 (신규 추가)

- [x] `Toggle` 공용 컴포넌트 신규 추가 (1단계에 없었음)
- [x] 닉네임 표시, 효과음/배경음/애니메이션 효과 감소 토글(localStorage 유지, 실제 오디오 시스템은 없음)
- [x] 로그아웃 - 확인 모달 추가 (기존 GameShell/HomePage에서는 확인 없이 즉시 실행했던 걸 정정, 설계서 2.4 "로그아웃 확인" 공통 모달 반영)
- [x] 회원 탈퇴 - 확인 모달, 확인 시 `GameStateContext` 초기화 + 로그아웃 (재화/보유아이템도 함께 리셋)
- [x] 이용약관, 개인정보처리방침, 버전 정보 (텍스트/자리표시자 수준)

버그 수정. `Toggle`을 `<label><button>` 구조로 만들었더니 라벨 텍스트를 클릭해도 안 눌림(label은 button을 자동 트리거하지 않음). 행 전체를 button 하나로 바꿔서 해결.

검증. 토글 클릭 반영, 로그아웃/회원탈퇴 확인 모달, 탈퇴 후 로그인 화면 복귀까지 Playwright 스크린샷. typecheck·build 통과.

## 4. 9단계 중 미션 탭

- [x] `shared/types/mission.ts` - MissionId, MissionInfo, MISSIONS 카탈로그(밥/물/애정표현/정원, product-plan.md 7장 수치 그대로)
- [x] MissionCard - 진행중/보상받기/완료 3가지 상태
- [x] `CollectionPage`에 "일일 미션" / "행동 도감" 상단 서브탭 구조 추가 (행동 도감 내용은 6번 작업에서 채움)
- [x] `GameStateContext`에 `missionProgress`, `claimMissionReward()` 추가. 보상받으면 재화 반영 + "완료" 도장으로 전환
- [x] 자정 리셋 로직 - 10단계에서 서버 `Mission.missionDate`(Asia/Seoul 기준)로 구현 완료
- [ ] 오늘 남은 미션 수 / 자정까지 남은 시간 표시 - 아직 없음 (아래 "남은 작업" 2번)

임시 시연용 초기 진행도. 케이지/정원이 없어 실제 행동으로 진행도가 안 쌓여서, 4가지 상태(진행중/보상받기/완료 2종)를 한 화면에서 다 보여주려고 미션마다 다른 초기값을 넣어둠(밥 2/3, 물 1/1, 애정표현 0/3, 정원 1/1 완료). 실제 게임플레이를 반영한 값이 아님 - 케이지/정원 액션이 생기면 전부 0부터 시작하도록 정정 필요.

검증. 4가지 상태 스크린샷, "물 채우기" 보상받기 클릭 -> 재화 100->110 반영 + 완료 도장 전환 + 토스트 확인. typecheck·build 통과.

## 5. 7단계 - 정원 화면

- [x] `shared/types/garden.ts` - PlotStatus(EMPTY/GROWING/READY), GardenPlot 타입. "씨앗 심음"은 설계서의 밭 정보 패널 구성상 GROWING과 동일하게 취급, 잡초는 GROWING 상태의 부가 플래그(`hasWeed`)로 모델링
- [x] GardenPlotTile - 4개 밭, 상태별 라벨 + 잡초 표시, 선택 시 테두리 강조
- [x] GardenActionSheet - 밭 선택 -> 하단 정보 패널 (빈밭/성장중(+잡초)/수확가능 별 다른 내용)
- [x] 심기 -> 잡초제거 -> 수확 흐름 (`GameStateContext`에 `plantSeed`/`removeWeed`/`harvestPlot`/`tickGardenGrowth`) + Toast로 보상 안내. 햄스터는 `HamsterSprite` 자리표시자를 정적으로 배치(실제 이동 애니메이션은 케이지 단계로 미룸)
- [x] 오프라인 진행 요약 모달 - localStorage에 마지막으로 본 날짜 저장, 하루 1회만 노출. 문구는 설계서 예시 그대로 고정(실제 이벤트 로그가 없어 트리거 로직만 구현, 내용은 10단계에서 실제 데이터로 교체)
- [x] 목업 성장 타이머 - lazy-tick을 흉내내어 심은 지 10초(데모용, 실제 값 아님) 지나면 자동으로 수확 가능 상태로 전환. `MOCK_GROW_DURATION_MS`로 명시

임시 시연용 초기 정원 상태. 4개 밭을 각각 빈밭/성장중/수확가능/성장중+잡초로 시작해서 모든 상태를 한 화면에서 보여줌. 실제 플레이 값이 아님.

검증. 오프라인 요약 모달, 4가지 밭 상태, 잡초 제거(씨앗 +1), 수확(재화 +5, 밭이 빈 상태로), 심기(씨앗 소모, 성장중으로 전환)까지 Playwright 스크린샷. typecheck·build 통과.

검증. 밭 상태 전환(빈밭->심기->성장중->수확) 스크린샷 순회

## 6. 9단계 중 행동 도감 탭

- [x] `shared/types/behavior.ts` - BEHAVIOR_INFO 12종(이름/힌트/설명, product-plan.md 6장 + hamster-character-info.md 7장 기준)
- [x] BehaviorCard - 미발견(실루엣 "?" + 힌트)/발견(`HamsterSprite`로 실제 이미지 자동 표시 + 설명 + 발견일) 상태
- [x] `GameStateContext`에 `discoveredBehaviors`, `discoverBehavior()` 추가
- [x] DiscoveryModal - 새 행동 발견 알림. 케이지가 없어 트리거할 방법이 없어서 "테스트. 다음 미발견 행동 발견하기" 버튼으로 임시 확인 (케이지 완성되면 실제 행동 실행 시 자동 호출로 교체하고 이 버튼은 제거)

임시 시연용 초기값. IDLE/WALK/EAT 3개만 미리 발견된 상태로 시작(자주 볼 행동이라 가정), 나머지 9개는 미발견. 실제로는 전부 미발견 상태로 시작해야 함 - 10단계에서 정정.

검증. 실제 이미지 3개(가만히 있기/걷기/먹기) + 실루엣 9개 동시 표시, 테스트 버튼으로 발견 트리거 -> 발견 모달 -> 카드가 실루엣에서 실제 이미지로 전환까지 Playwright 스크린샷. typecheck·build 통과.

## 중간 통합 확인 (보류 그룹 시작 전)

- [x] 로그인 -> 케이지 -> 정원 -> 상점 -> 도감(미션/행동) -> 설정 -> 케이지, PC(1280px)·모바일(390px) 각각 한 세션 안에서 전체 순회
- [x] 콘솔 에러 확인 - 파비콘 404(무해) 외 없음
- [x] `npm run typecheck`, `npm run build -w client` 통과

캐릭터 에셋 없이 진행 가능한 6개 작업(공통 레이아웃/상점/설정/미션/정원/행동도감)이 모두 끝났다. 여기서부터는 온보딩/케이지/캐릭터 애니메이션(보류 그룹)이고, 골든 12포즈 + 다른 색상이 채워지는 대로 이어간다.

---

## 보류였던 것 - 골든·그레이 에셋 확보 후 해제 (2026-08-02)

### 2단계 나머지 - 온보딩
- [x] 온보딩 1/2단계(외형 선택, 이름 입력), 로그인 성공 시 `/home` vs `/onboarding` 분기 - 아래 "2026-08-02 전체 구현 계획" 2번에서 완료
- [ ] StartPage - 아직 없음. `/`가 AuthPage로 직행하고 로그인/회원가입은 한 화면 모드 전환 (아래 "남은 작업" 1번)

### 4, 5, 6단계 - 케이지 화면
- [x] CageStage(1000x700, 비율 좌표), 레이어 순서, 기본 가구 배치, 상태바, 액션 버튼
- [x] 캐릭터 애니메이션(대기/걷기/쳐다보기/먹기/물마시기/잠자기/쓰다듬기), 좌우 반전
- [x] 액션 -> 이동 -> 행동 -> 상태 변경, 꾸미기 모드, 배치 제한, 클릭 반응, 말풍선, 상세 정보 모달

전부 아래 "2026-08-02 전체 구현 계획" 3, 5, 7, 9번에서 완료. 배치 제한은 꾸미기 모드의 안전 좌표 clamp로 처리.

## 10단계 - 실제 API 연결

순서. 인증 -> 사용자 정보 -> 햄스터 상태 -> 케이지 데이터 -> 햄스터 액션 -> 정원 -> 상점 -> 미션 -> 행동 도감.

- [x] Docker/WSL2 정상화 확인, `docker compose up -d`로 로컬 Postgres 기동, 루트 `.env` 생성
- [x] `prisma migrate dev --name init` 적용
- [x] User 모델 email -> nickname 전환 (`schema.prisma`, `shared/types/auth.ts`, `server/src/routes/auth.ts`)
- [x] 실제 회원가입 -> 로그인 -> 쿠키 인증(`/me`) curl로 확인. `tester`/`1234` 계정 DB에 실제로 존재함
- [x] `client/src/lib/api.ts` 복원 - 닉네임 기준으로 signup/login/logout/fetchMe
- [x] `AuthContext`를 실제 API로 전환 - 앱 시작 시 `/api/auth/me`로 세션 확인(`isLoading`), `login`/`signup`은 이제 에러를 던지고 서버 메시지를 그대로 화면에 표시, `logout`도 실제 쿠키 삭제 호출
- [x] `mockAuth.ts` 삭제 (더 이상 참조하는 곳 없음)
- [x] `ProtectedRoute`, `AuthPage`에 세션 확인 중 로딩 화면(`LoadingHamster`) 추가 - 로그인된 사용자가 새로고침할 때 로그인 폼이 잠깐 보이는 깜빡임 방지
- [x] `SettingsPage`의 로그아웃/탈퇴를 `await logout()`으로 정정 (기존엔 동기 호출이었음)
- [x] 실제 검증. 틀린 비밀번호 -> 서버 에러 메시지 표시 / 올바른 로그인 -> `/home` 이동 / 새로고침 -> 세션(httpOnly 쿠키) 유지 / 로그아웃 -> 쿠키 실제로 삭제(`document.cookie`로 접근 불가, `httpOnly: true` 확인) 전부 Playwright로 확인. typecheck·build 통과.

알아둘 것 (해결됨). `GameStateContext`의 재화(currency)가 서버 `User.currency`와 연결 안 돼 있던 문제는 아래 "사용자 정보 / 케이지 데이터 ..." 단계의 `GameStateContext` 전면 재작성으로 정리됨. 이제 재화는 `GET /api/state` 응답만 따른다.

### 사용자 정보 / 케이지 데이터 / 정원 / 상점 / 미션 / 행동 도감 - 전부 실제 API로 전환 완료

"햄스터 상태"와 "햄스터 액션"은 Hamster 모델이 온보딩(보류 그룹) 전까지 없어서 건너뛰었다. product-plan.md 데이터 모델상 CageItem/GardenPlot/Mission/BehaviorLog는 원래 User의 직접 자식이라(Hamster 경유 아님) 온보딩 없이도 구현 가능했다.

- [x] Prisma 스키마 확장 - `ItemMaster`(정적 카탈로그, enum ItemId), `CageItem`, `GardenPlot`(enum PlotStatus), `Mission`(enum MissionId, `missionDate`로 일일 리셋), `BehaviorLog`(enum HamsterBehavior). `User.seedCount` 필드 추가. 2번의 마이그레이션 적용
- [x] `seedItemMasters()` - 서버 부팅 시 아이템 카탈로그 upsert (idempotent)
- [x] 회원가입 시 `initializeStarterData()` - 기본 가구 3종, 정원 4칸(EMPTY) 자동 생성. 실제 신규 유저는 전부 깨끗한 상태로 시작 (목업 시절의 "시연용" 값과 다름 - 정정 완료)
- [x] `GET /api/state` - 재화/씨앗/보유아이템/정원/미션(오늘자, lazy 생성)/도감을 한번에 반환. 정원 성장도 조회 시점에 lazy-tick으로 계산(`tickGardenGrowth`, 10분 - product-plan.md에 실제 수치 없어 임시값)
- [x] `POST /api/shop/purchase`, `POST /api/garden/:plotIndex/plant`·`/remove-weed`·`/harvest`, `POST /api/missions/:missionId/claim`, `POST /api/behaviors/:behaviorId/discover` - 전부 트랜잭션으로 처리하고 최신 `GameStateResponse`를 반환
- [x] `shared/types/gameState.ts` 신규 - API 응답 타입 통일
- [x] 프론트 `GameStateContext` 전면 재작성 - `localStorage` 목업 제거, 로그인되면 `/api/state`로 부트스트랩, 각 액션은 서버 호출 후 응답으로 상태 교체. `useGameState()`(로드 후 사용, throw 가능)와 `useGameStateStatus()`(GameShell의 로딩 게이트용) 두 훅으로 분리
- [x] `GameShell`에 로딩 게이트 추가 - 상태 로드 전엔 `LoadingHamster`
- [x] `ShopPage`/`GardenPage`/`CollectionPage` 전부 async + try/catch로 전환, 서버 에러 메시지를 그대로 토스트로 표시
- [x] `SettingsPage`의 회원 탈퇴 - 실제 계정 삭제 API가 아직 없어서 지금은 로그아웃과 동일하게 동작 (주석으로 명시)
- [x] 검증. 신규 회원가입 -> 초기 상태(재화100/씨앗1/기본가구3/정원4빈밭/미션0/도감0) 확인 -> 상점 구매(재화 정확히 소진) -> 정원 심기(씨앗 소진, 성장중 전환) -> 새로고침해도 DB에 저장된 상태 유지 -> 도감 발견까지 Playwright로 실제 회원가입한 계정으로 전체 확인. typecheck·build 통과.

## 다음 - 온보딩/케이지 (골든·그레이 2색상 기준으로 진행)

## 2026-08-02 전체 구현 계획

### 1. 에셋 및 기반 정리
- [x] 진행 중인 햄스터 에셋 이동 상태 확인 (골든 12개, 그레이 16개)
- [x] 현재 기준 타입 검사 및 프로덕션 빌드 통과 확인
- [x] 스프라이트 선택을 안정화하고 누락 행동 fallback 추가

### 2. 햄스터 온보딩
- [x] Prisma Hamster 모델 및 마이그레이션 추가
- [x] 온보딩 조회/생성 API와 공유 타입 추가
- [x] 골든·그레이 외형 선택, 이름 입력 화면 구현
- [x] 햄스터 생성 여부에 따른 `/onboarding`과 `/home` 라우팅 분기

### 3. 케이지 핵심 플레이
- [x] 케이지 상태를 게임 상태 API에 포함
- [x] 먹이, 물, 쓰다듬기, 청소 액션 API 구현
- [x] CageStage, 상태바, 가구 및 햄스터 렌더링 구현
- [x] 액션 중 이동/행동 연출과 결과 피드백 구현
- [x] 햄스터 상세 정보 모달 구현

### 4. 도메인 연동
- [x] 햄스터 액션에서 미션 진행도 자동 증가
- [x] 정원 행동에서 정원 미션 진행도 자동 증가
- [x] 실제 행동 실행에서 행동 도감 자동 발견
- [x] 행동 도감의 임시 발견 버튼 제거

### 5. 오프라인 진행 및 꾸미기
- [x] 최대 3일 lazy-tick과 햄스터 상태 감소/수면 전환 구현
- [x] 한국 시간 기준 일일 미션 날짜 계산
- [x] 가구 위치 변경 API와 케이지 꾸미기 모드 구현

### 6. 품질 및 배포 준비
- [x] 핵심 도메인 로직 자동 테스트 추가
- [x] 서버 빌드 및 전체 테스트 스크립트 추가
- [x] API 오류 처리와 환경변수 검증 추가
- [x] 앱 포함 Docker 구성과 배포용 설정 추가
- [x] 전체 타입 검사, 테스트, 빌드 및 핵심 흐름 검증

### 7. 실제 애니메이션 프레임
- [x] `walk1`, `walk2`를 프레임이 아닌 별도 걷기 시안으로 구분
- [x] 골든 `walk-01` 기준 6프레임 보행 사이클 제작
- [x] 그레이 `walk-01` 기준 6프레임 보행 사이클 제작
- [x] 512x512 개별 투명 PNG로 분리 및 alpha 검증
- [x] 이동 중 120ms 간격 프레임 재생 연결
- [x] 타입 검사 및 프로덕션 빌드 통과

### 8. 케이지 기본 가구 이미지
- [x] 픽셀 아트 물병 이미지 제작 및 투명 PNG 변환
- [x] 픽셀 아트 잠자는 집 이미지 제작 및 투명 PNG 변환
- [x] 픽셀 아트 밥그릇 이미지 제작 및 투명 PNG 변환
- [x] 기존 행동 이미지를 기준으로 빈 쳇바퀴 이미지 제작
- [x] 케이지 이모지를 실제 가구 이미지로 교체
- [x] 기존 계정의 누락된 기본 가구 자동 보충

### 9. 물 가구 선택 및 자율 이동
- [x] 손에 쥐는 젖병형 물병 이미지 제작 및 투명 PNG 변환
- [x] 낮은 물그릇 이미지 제작 및 투명 PNG 변환
- [x] 벽걸이 물병, 젖병형, 물그릇을 별도 DB/상점 아이템으로 구성
- [x] 세 종류 모두 물 주기 행동 대상으로 연결
- [x] 꾸미기 모드에 포인터 드래그 배치 추가
- [x] 케이지 관찰 중 햄스터 자율 걷기와 방향 전환 추가
- [x] 타입 검사, 테스트, 클라이언트/서버 빌드 통과
- [x] 보유 쳇바퀴까지 이동해 일정 시간 사용하는 자율 행동 추가
- [x] 집 입구까지 이동해 안에서 쉬고 다시 나오는 자율 행동 추가
- [x] 받침은 고정되고 원형 바퀴만 회전하는 쳇바퀴 4프레임 제작 및 재생

---

## 햄스터 클릭으로 쓰다듬기 (2026-08-02)

사용자 요청. "햄스터 클릭하면 쓰다듬는거 할수있으면 좋겠어. 애니메이션 효과가 들어가면 더 좋을것 같아."

- [x] 케이지의 햄스터 클릭 -> 쓰다듬기. 원래 클릭은 상세 모달을 열었는데 상태 패널에 "자세히 보기" 버튼이 이미 있어 클릭을 넘겼다
- [x] 연출 3종 - 쓰다듬기 스프라이트(사람 손 포함), 몸이 눌렸다 돌아오는 애니메이션, 머리 위로 떠오르는 하트
- [x] 연달아 눌러도 토스트가 쌓이지 않도록 성공 안내 생략(`act`의 `silent` 옵션), 오류는 그대로 표시
- [x] `prefers-reduced-motion`이면 연출 정지
- [x] screen-design.md의 "쓰다듬기 모드" 기술을 클릭 방식으로 정정

CSS 재생 함정 두 가지. 래퍼에 `key`를 주면 `HamsterSprite`까지 리마운트돼 깜박임과 걷기 프레임이
초기화된다(클래스 토글로 변경). 눌리는 연출을 버튼에 직접 걸면 위치 이동용 transform과 충돌한다(안쪽 div로 분리).

검증. 클릭 -> 하트/눌림 연출/쓰다듬기 스프라이트 표시, 상세 모달 안 열림, 애정 표현 미션 3/3 도달 확인.

---

# 다음 할 일 (2026-08-02 기준)

아래 항목이 현재 남아 있는 전부다. 위쪽의 "완료" 기록들은 이 아래 섹션들에 있다.

## A. 코드로 지금 할 수 있는 것

### A-1. 도감에서 영영 발견할 수 없는 행동 3종

- [ ] 가만히 있기(`IDLE`), 걷기(`WALK`), 사용자 쳐다보기(`USER_LOOK`)

화면에서는 실제로 계속 연출되는데 등록 경로가 없다. 현재 발견되는 것은 돌보기 4종(먹기/물마시기/쓰다듬기/
세수하기), 정원 작업, 자율 행동 5종(쳇바퀴/잠자기/세수하기/볼에저장/둘러보기)뿐이다.
`POST /api/behaviors/:id/discover`는 이미 있는데 부르는 곳이 없다. 12종 중 3종이 수집 불가라 도감이 안 채워진다.

### A-2. 성격과 성장 단계가 고정값

- [ ] 성격은 생성 시 랜덤 배정 + 친밀도 30 이상에서 공개 (product-plan.md 6장)
- [ ] 성장 단계는 항상 `BABY`. 변화 조건이 정해진 바 없음

지금은 둘 다 상세 정보 모달에 그대로 노출된다. 성격은 전원 "느긋함"이다.

### A-3. 설정의 효과음/배경음/애니메이션 토글이 아무 동작 안 함

- [ ] localStorage에 저장만 되고 읽는 곳이 없다. 오디오 시스템이 없어서 최소한 애니메이션 감소만이라도 연결하거나, 아니면 토글을 감춰야 한다

### A-4. 모바일 케이지가 작음 (선택)

- [ ] 모바일에서 스테이지가 334px. 비율은 PC와 같지만 절대 크기가 작다. 상태바를 접거나 스테이지를 화면 가장자리까지 넓히는 방법이 있다

## B. 제품 결정이 필요한 것 (코드로는 더 못 감)

### B-1. 밸런스 수치 확정

- [ ] 정원 작물 성장 시간 - 현재 10분(`GROW_DURATION_MS`), product-plan.md에 수치 없음
- [ ] 잡초 생성 주기 - 현재 5분(`WEED_INTERVAL_MS`), 임의로 정함
- [ ] 자율 행동 회복량(+5~8)과 쿨다운(20초)
- [ ] 돌보기 액션(특히 쓰다듬기)의 서버 쿨다운 - 지금은 없다. 클라이언트가 약 1.2초 막지만 API를 직접
      부르면 제한이 없다. 수치는 100에서 잘리고 미션도 목표에서 멈춰 큰 문제는 아니다

전부 상수 하나씩만 바꾸면 된다. 위치는 `server/src/lib/gameState.ts`, `server/src/lib/balance.ts`,
`server/src/routes/hamster.ts`.

### B-2. 캐릭터 외형 5종

- [ ] 화이트, 베이지, 브라운, 차콜, 크림. 지금은 골든/그레이 2종뿐이라 온보딩에서 2개만 고를 수 있다

### B-3. 대기 상태 프레임 (선택)

- [ ] 숨쉬기 정도의 미세한 대기 애니메이션. 생기면 "연속 휴식 1회 제한"을 풀어 더 느긋하게 둘 수 있다

## C. 출시 전 필수

### C-1. 약관 법률 검토

- [ ] 이용약관과 개인정보처리방침이 초안이다. 두 화면 상단에 그 사실을 명시해뒀지만 공개 전 반드시 다시 써야 한다

### C-2. react-router 취약점 재확인

- [ ] GHSA-qwww-vcr4-c8h2. 2026-08-02 기준 수정 버전이 없다(영향 범위 7.12.0~8.2.0, npm 최신 7.18.2).
      RSC 모드를 안 써서 해당 없음으로 유지 중. 배포 때 다시 확인

### C-3. 실제 배포

- [ ] Docker Compose와 GitHub Actions는 준비돼 있으나 실제 서버(Oracle)에 올린 적이 없다

---

## 남은 작업 정리 (2026-08-02) - 전부 완료

위쪽 체크박스를 실제 코드와 대조해 남은 것을 5개로 추린 뒤 순서대로 처리했다. 보류 그룹(온보딩/케이지/애니메이션)은 이미 전부 해제된 상태였고, 아래가 마지막으로 남아 있던 항목들이다.

### 1. 정원 오프라인 요약 모달을 실제 데이터로 교체

- [x] 잡초 생성 로직 신규 구현 - product-plan.md 오프라인 진행 항목에 "잡초 생성"이 있는데 코드가 없어서 `hasWeed`가 영원히 false였고 잡초 제거 기능도 도달 불가였음. `GardenPlot.weedFrom` 기준 5분(`WEED_INTERVAL_MS`, 성장 10분의 절반)으로 결정론적 생성
- [x] `tickGardenGrowth`가 성장 완료/잡초 생성 개수를 반환하도록 변경
- [x] 자리를 비운 사이(30분 이상, 수면 전환 기준과 동일)의 변화만 `User.pendingGrownCount`/`pendingWeedCount`/`pendingSummaryFrom`에 누적. 화면을 보고 있는 동안의 변화는 모달로 끊지 않는다
- [x] `GET /api/state`에 `gardenSummary` 추가, 변화가 없으면 `null`이라 모달 자체가 안 뜸
- [x] `POST /api/garden/summary/ack` - 확인하면 누적분을 비운다. 기존 localStorage 하루 1회 게이트는 제거(서버가 기준이 됨)
- [x] 모달 문구를 실제 개수로 생성 + 자리 비운 기간 표현("2일 만에 와보니")
- [x] 마이그레이션 `20260802093421_garden_weed_and_offline_summary` - 기존 GROWING 밭은 `weedFrom = plantedAt`으로 백필

버그 수정. 로그인 라우트가 `lastActiveAt`을 즉시 갱신해서 오프라인 간격이 사라지고 있었다. 그래서 정원 소식뿐 아니라 **햄스터 상태 감소/수면 전환도 로그인 직후 첫 조회에서 반영되지 않았다.** 갱신은 `tickHamsterState`에만 맡기도록 제거.

### 2. 미션 탭 상단 요약 표시

- [x] `msUntilKoreaMidnight()` 추가 - 자정 판정을 클라이언트가 따로 계산하지 않고 서버 기준(`missionResetInMs`)을 받아 쓴다
- [x] `MissionSummary` 컴포넌트 - "오늘 남은 미션 N개 / 자정까지 H시간 M분", 1초 간격 카운트다운
- [x] 남은 미션 = 보상을 아직 받지 않은 미션 수

### 3. 시작 화면(StartPage) 분리

- [x] `AuthPage`(로그인/회원가입 한 화면 모드 전환) 삭제하고 `/` `/login` `/signup` 3분리
- [x] StartPage - 로고, 소개 문구, 회원가입/로그인 버튼, 약관 링크. 햄스터가 좌우로 걸어다니다 멈춰 서고 PC에서만 가끔 사용자 쪽을 바라본다. 모바일은 걷기 한 종류만, `prefers-reduced-motion`이면 정지 (설계서 6.1)
- [x] LoginPage - 비밀번호 표시 토글 추가 (설계서 6.2)
- [x] SignupPage - 비밀번호 확인, 필수 약관 동의 체크 추가 (설계서 6.3)
- [x] 로그인 성공 시 `/home`으로 보내고 햄스터가 없으면 GameShell이 `/onboarding`으로 넘긴다. 가입 직후는 바로 `/onboarding`

### 4. 회원 탈퇴 실제 API

- [x] `DELETE /api/auth/me` - 계정 삭제 + 인증 쿠키 삭제. 연관 데이터는 스키마의 `onDelete: Cascade`로 함께 삭제
- [x] `AuthContext.deleteAccount()`, SettingsPage 탈퇴 모달을 실제 호출로 연결(처리 중 로딩, 실패 시 토스트)

### 5. 이용약관 / 개인정보처리방침

- [x] `/terms`, `/privacy` 화면 신규 - 로그인 전에도 읽을 수 있도록 보호 라우트 밖에 배치
- [x] 설정 화면의 빈 버튼과 시작/회원가입 화면 링크를 여기로 연결
- [x] 개인정보처리방침은 실제로 저장하는 항목(닉네임, 비밀번호 해시, 게임 진행 데이터, httpOnly 쿠키)만 기재

주의. 두 문서 모두 화면 상단에 "출시 전 초안, 법률 검토 필요"를 명시해뒀다. 실제 공개 전에 반드시 다시 작성해야 한다.

## 케이지 연출 다듬기 (2026-08-02)

사용자 피드백 세 가지.

- [x] 모래목욕통 - 통 앞에 서던 것을 통 안 모래 위에 올라앉는 걸로 변경(`offsetY: -0.19`),
      표시 크기 `w-20 md:w-32` -> `w-40 md:w-64` (참고 이미지 비율: 햄스터 몸 폭 = 통 폭의 약 1/3)
- [x] 집에서 잘 때 사라지고 `Z z z` 말풍선이 뜨던 것을 집 앞에서 자는 걸로 변경. `isInHouse`,
      `houseActivityPosition` 상태와 말풍선 제거하고 다른 가구와 같은 경로로 통일(`sleep.png`에 z 표시가 이미 있음)
- [x] 가구별 `offsetX`/`offsetY` 추가 - 집은 위쪽 벽에 놓이므로 아래로 0.16 내려 선다. 가구 행동은 y 0.35까지 허용
- [x] 걷다가 밥 앞에서 작아 보이던 문제 - `SCALE_OVERRIDES`에 걷기 0.90 / 정면 자세 1.06 추가

### 케이지 스테이지 반응형 정리

위 작업 중 발견해 이어서 처리. 햄스터는 `size={160}` 고정인데 가구는 반응형 px 폭이라 화면 크기에 따라
둘의 비율이 달라졌다. 모바일에서 햄스터만 커 보였다.

- [x] 스테이지 폭을 `ResizeObserver`로 재고 햄스터·가구 크기를 전부 그 비율로 계산 (`widthRatio`, `HAMSTER_WIDTH_RATIO`)
- [x] 첫 페인트 깜빡임 방지를 위해 `useLayoutEffect`에서 측정
- [x] 스테이지 `min-h-72` 제거 - 좁은 화면에서 10:7 비율이 깨져 위치 계산과 어긋났다
- [x] 좌우 분할을 `md:grid-cols-[240px_1fr]` -> `lg:grid-cols-[220px_1fr]` - md에서 나누면 좌측 메뉴와 겹쳐 820px 화면에서 스테이지가 276px까지 줄었다(현재 532px)

측정. PC 756 / 태블릿 532 / 모바일 334px 모두 비율 1.43, 스테이지 대비 햄스터 0.213~0.214,
집 0.191~0.194, 모래목욕통 0.340~0.344로 일치.

크기 문제의 원인. 걷기는 옆모습이라 몸이 길게 퍼지고 먹기·서있기는 정면이라 뭉쳐 보인다. 면적을 같게
맞췄더니 실루엣 폭이 351 대 272로 23% 차이나서 걷다 멈추면 작아진 것처럼 보였다. 양쪽을 가운데로 당겨
폭 차이를 10%로 줄였다.

이 과정에서 발견. `gen-hamster-blink.py`의 눈 좌표가 하드코딩이라 정규화 배율을 바꾸면 조용히 어긋난다.
어두운 픽셀 덩어리 중 크기와 높이가 비슷한 좌우 한 쌍을 찾는 자동 검출로 교체했다.

## 대기 상태 눈 깜박임 (2026-08-02)

사용자 피드백. "가만히 서있을때 눈 깜박이는것도 하면 좋겠네."

- [x] `scripts/gen-hamster-blink.py` - `idle.png`의 눈을 지우고 감은 눈꺼풀을 그려 `blink.png` 생성(골든/그레이)
- [x] `HamsterSprite`가 `IDLE`일 때만 1.8~4.8초마다 140ms 깜박임. 20% 확률로 두 번 연달아(3회 이상은 차단)
- [x] `<img>`의 `key={src}` 제거 - 그림이 바뀔 때마다 새로 마운트돼 한 프레임씩 깜빡였다

프레임 배열이 아니라 그림 한 장을 바꿔치기하는 이유. 깜박임은 "오래 뜨고 아주 잠깐 감는" 리듬이라
일정 간격으로 도는 프레임에 담으면 대부분이 뜬 눈 프레임이 되어 낭비다.

검증. 40초 관찰 - 깜박임 6회, 걷기/잠자기 중에는 발생하지 않음, 이중 깜박임 동작 확인.

## 햄스터 스프라이트 규격 정규화 (2026-08-02)

사용자 피드백. "걸어다니는 이미지 크기, 가만히 서있는 크기, 밥먹는 크기, 물마시는 크기가 왜 다 다르냐."

원인은 코드가 아니라 에셋. `HamsterSprite`는 전부 같은 정사각 상자에 그리는데 원본이 제각각이었다.
캔버스 비율(1536x1024 / 512x512 / 1024x1024)이 달라 3:2 그림만 가로로 눌렸고, 캔버스 안에서 햄스터가
차지하는 비율도 0.31~0.69로 제각각이라 화면상 크기가 2배 넘게 차이났다.

- [x] `scripts/normalize-hamster-sprites.py` 신규 - 알파로 잘라내고 불투명 면적의 제곱근 기준으로 크기를 맞춰 512x512 캔버스에 가로 중앙 + 바닥 정렬
- [x] 소품이 함께 그려진 파일(pet 손 / wheel 쳇바퀴 / drink 물병)은 `SCALE_OVERRIDES`로 파일별 보정
- [x] 43개 스프라이트 전부 재저장, 12종 도감과 케이지에서 크기 일관성 확인

면적을 기준으로 삼은 이유. 가로나 세로 하나만 쓰면 걷는 자세처럼 납작한 그림이 과하게 커지거나 작아진다.
면적은 자세가 바뀌어도 몸집이 일정해 안정적이다. 바닥 정렬은 걷다 멈춰도 발 위치가 튀지 않게 하려는 것.

함정 두 가지. `walk2.png`는 배경 제거 후 남은 옅은 픽셀이 흩어져 있어 단순 `getbbox()`가 캔버스 전체를
반환했다(행/열 픽셀 수 기반으로 변경해 해결). `walk-01/sheet-source.png`는 프레임 원본 시트라 제외 대상.

부수 효과. `assets/hamsters` **69MB -> 7.9MB**, 개별 파일 2.2MB -> 115~152KB. 160px로 표시하는
스프라이트가 1536x1024 원본 그대로였다.

주의. 스크립트는 원본 사본을 입력으로 받는다. 원본을 덮어쓰며 반복 실행하면 리샘플링이 누적된다.

## 케이지 자율 행동 템포 조정 (2026-08-02)

사용자 피드백. "행동이 너무 빠르다. 한 번 걸을 때 한 행동이 아니라 걷는 걸 여러 번 해도 된다."

- [x] 가구 행동 확률을 각 16/14% -> 각 5%로 하향 (5종 다 보유해도 25%)
- [x] `ACTIVITY_MIN_GAP` - 가구 행동 뒤 최소 2번은 걷기/쉬기만 (확률만 낮추면 연속으로 나올 수 있어서 별도 장치)
- [x] 동작 사이 대기 0.5~1.4초 -> 2~5초
- [x] 걷기를 거리 비례 시간으로 계산해 속도를 일정하게 하고, 속도 자체도 하향(`WALK_SPEED_PER_MS`)
- [x] 목적지를 케이지 전체 랜덤 -> 현재 위치 근처(`wanderTarget`)로 변경
- [x] 30% 확률로 걷지 않고 제자리에서 쉬는 구간 추가 (연속 쉬기는 1회로 제한)
- [x] 걷기 프레임 간격 200ms -> 360ms (쳇바퀴는 160ms 유지)
- [x] 돌보기 액션 후 케이지 가운데로 순간이동하던 동작 제거

조정 중 발견. 연속 쉬기가 겹치면 16초 넘게 완전히 멈춰 있어 고장 난 것처럼 보였다. 정지 스프라이트라
변화가 아예 없기 때문이다. 연속 쉬기를 1회로 제한해 최대 9초로 줄였다. 대기 상태용 프레임이 생기면 풀어도 된다.

검증. 75초 관찰 측정 - 걷기 1.3~2.9초, 서 있기 3.8~9.0초, 가구 행동 32초 간격. 이전에는 가구 행동이
약 4초마다 발생해 서버 쿨다운(20초)에 대부분 막혀 수치가 반영되지 않았는데, 이제 간격이 쿨다운을 넘겨
실제로 청결/기분이 오른다. typecheck·test·build 통과.

## 터널 교체와 세수하기 명칭 정리 (2026-08-02)

사용자 지적으로 확인. 터널은 상점 최고가(150재화)인데 이미지도 행동도 상태 효과도 없어 사면 재화만
사라지는 아이템이었다. 액션 버튼 "청소하기"는 실제 동작이 전부 세수하기였다.

### 터널 -> 놀이 가구 3종

- [x] `sand-bath.png` / `snack-dish.png` / `lookout.png` 픽셀 아트 제작 (`scripts/gen-cage-items.py`로 재생성 가능)
- [x] 모래목욕통(세수하기, 청결 +8) / 해바라기씨 접시(볼에 저장, 기분 +6) / 전망대(서서 둘러보기, 기분 +5), 각 120재화
- [x] `POST /api/hamster/idle-activity` 신규 - 가구 보유 확인, 수치 반영, 행동 도감 등록. `Hamster.lastIdleAt` 기준 20초 쿨다운
- [x] CagePage 자율 행동을 `IDLE_ACTIVITIES` 표 기반으로 정리 - 보유한 가구만 확률 구간을 차지한다
- [x] 마이그레이션 - enum에서 TUNNEL 제거, 이미 산 사용자의 터널은 같은 값어치의 모래목욕통으로 교체

부수 효과. 쳇바퀴/집 자율 행동도 이제 도감에 등록된다. "볼에 저장"과 "서서 둘러보기"는 도감에 있으면서
발견할 방법이 아예 없던 행동이었다.

버그 수정. 상점에서 산 가구가 전부 기본 좌표(0.5, 0.5)에 생성돼 케이지 한가운데 겹쳐 쌓였다.
`pickFreeSlot()`으로 비어 있는 후보 자리에 놓도록 수정.

개선. 상점 카드가 이름 텍스트 자리표시자였던 것을 실제 가구 그림으로 교체(`lib/cageItemAssets.ts` 공용화).

### 청소하기 -> 세수하기

- [x] 액션 버튼 라벨과 API 액션 id를 `CLEAN` -> `WASH`로 통일 (DB 저장값이 아니라 요청 enum이라 마이그레이션 불필요)
- [x] product-plan.md, screen-design.md의 "청소" 기술을 세수하기로 정정

설계서 6.5의 "케이지 전체 청소"(햄스터가 피하거나 구경) 연출은 에셋도 로직도 없이 이름만 남아 있던
것이라 MVP 범위에서 제외로 명시했다.

검증. 신규 3종 구매 -> 케이지에 겹치지 않게 배치 -> 자율 행동으로 도감 4종(서서 둘러보기/잠자기/
볼에 저장/세수하기) 등록 확인. 쿨다운 중 재요청 시 수치 불변, 미보유 가구 요청 400, 구 액션 id
`CLEAN` 400 확인. typecheck·test·build 통과.

## 배포 전 재확인 (2026-08-02 확인 결과)

- [x] `react-router-dom` 취약점(GHSA-qwww-vcr4-c8h2) 재확인 - **수정 버전이 아직 없다.** 권고 영향 범위가 7.12.0~8.2.0이고 npm 최신도 7.18.2라 올라갈 곳이 없으며, npm이 제안하는 유일한 "수정"은 7.11.0 다운그레이드(breaking). 이 프로젝트는 Vite SPA + 클라이언트 라우팅만 쓰고 RSC 모드를 쓰지 않아 해당 없음으로 유지. 다음 배포 때 다시 확인
- [ ] 정원 성장 10분(`GROW_DURATION_MS`), 잡초 5분(`WEED_INTERVAL_MS`)은 product-plan.md에 수치가 없어 정한 임시값. **제품 결정이 필요한 항목이라 코드로는 더 진행할 수 없다.** 밸런스 수치가 정해지면 두 상수만 바꾸면 된다

## 검증 (2026-08-02)

- `npm run typecheck`, `npm test`(6건), `npm run build`, `npm run build:server` 전부 통과
- API. 가입 -> 햄스터 생성 -> 심기 -> 2시간 부재 시뮬레이션 -> `gardenSummary` 정확히 누적(재조회해도 중복 누적 없음) -> ack 후 null -> 잡초 제거(씨앗 +1) -> 수확(재화 +5) 확인
- 계정 삭제. `DELETE /api/auth/me` 204 -> `/me` 401, 연관 테이블 고아 행 0건 확인
- 화면(Playwright, PC 1280 / 모바일 390). 시작 화면 캐릭터 연출, 약관/개인정보 화면, 회원가입 검증(비밀번호 불일치/약관 미동의), 로그인 비밀번호 표시 토글, 2일 부재 후 정원 소식 모달 문구가 실제 밭 상태와 일치, 확인 후 새로고침해도 다시 안 뜸, 미션 요약("오늘 남은 미션 3개 / 자정까지 5시간 11분"), 설정에서 탈퇴 -> 시작 화면 복귀 + DB에서 계정 삭제까지 확인
- 콘솔 에러는 파비콘 404와 로그인 전 `/api/auth/me` 401 외 없음

## 운영 접근과 배포 자동화 (2026-08-12)

- [x] `docker-compose.prod.yml`의 caddy 서비스 제거, `Caddyfile` 삭제 (실서버는 호스트 nginx가 80/443을 잡고 있어 포트가 겹친다)
- [x] app을 `0.0.0.0:3000` -> `127.0.0.1:3000`으로 바꿔 HTTPS 우회 접근 차단
- [x] `docs/deployment.md`를 실서버 nginx + certbot 구성에 맞춰 재작성
- [x] db를 `127.0.0.1:5432`에 게시 (공인 IP에는 열지 않는다)
- [x] `scripts/db-tunnel.sh` 추가 - 로컬 55432 -> 서버 루프백 5432 SSH 터널
- [x] `scripts/deploy.sh` 추가 - 백업 -> origin/main 갱신 -> 재빌드 -> 헬스체크 -> 실패 시 자동 롤백
- [ ] 브랜치를 `origin/main` 위로 rebase 후 PR 생성, CI 통과 확인, 머지
- [ ] `./scripts/deploy.sh` 실제 실행 (서버 쓰기 작업은 사용자가 직접 수행)
- [ ] `./scripts/db-tunnel.sh`로 로컬 GUI 툴 접속 확인
- [ ] nginx에 보안 헤더 4종과 gzip 추가 (문서에는 반영, 서버 미적용)
- [ ] `.env.production.example`에서 미사용이 된 `DOMAIN`, `ACME_EMAIL` 제거 (Caddy 전용이었다)

검증. `sh -n`으로 두 스크립트 문법 확인, `docker compose -f docker-compose.prod.yml config`로
app과 db 모두 `host_ip: 127.0.0.1`인지 확인, `npm test` 6건 통과.
