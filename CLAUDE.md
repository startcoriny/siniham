# CLAUDE.md

이 프로젝트에서 Claude가 가장 먼저 읽는 규칙 문서입니다.

**에이전트 구성:** 이 프로젝트는 Claude 단독으로 진행합니다. 별도의 Planning / Implementation / Review 에이전트를 분리하지 않고, Claude가 세 단계를 순차적으로 수행합니다. 자세한 흐름은 [docs/rules/workflow.md](docs/rules/workflow.md)를 참조합니다.

**트레이드오프:** 이 가이드라인은 속도보다 신중함에 치우쳐 있습니다. 단순한 작업에서는 상황에 맞게 판단합니다.

---

## 1. 코딩 전에 먼저 생각하기

**추측하지 않습니다. 혼란을 숨기지 않습니다. 트레이드오프를 드러냅니다.**

구현 전에 확인합니다.

- 자신의 가정을 명확하게 설명합니다. 확실하지 않다면 질문합니다.
- 여러 해석이 가능하다면 모두 제시합니다. 조용히 하나를 선택하지 않습니다.
- 더 단순한 접근 방식이 있다면 말합니다. 필요하다면 반대 의견도 제시합니다.
- 불명확한 부분이 있다면 멈춥니다. 무엇이 헷갈리는지 설명하고 질문합니다.

## 2. 단순함 우선

**문제를 해결하는 최소한의 코드만 작성합니다. 추측성 구현은 하지 않습니다.**

- 요청받지 않은 기능 추가 금지.
- 한 번만 사용할 코드를 위한 추상화 금지.
- 요청받지 않은 "유연성" 또는 "설정 가능성" 추가 금지.
- 발생할 수 없는 상황을 위한 에러 처리 금지.
- 200줄로 작성했는데 50줄로 가능하다면 다시 작성합니다.

스스로에게 물어봅니다. "시니어 엔지니어가 이걸 보고 과하다고 할까?" 그렇다면 단순화합니다.

## 3. 외과적 수정

**필요한 부분만 수정합니다. 자신이 만든 부분만 정리합니다.**

기존 코드를 수정할 때 지킵니다.

- 주변 코드, 주석, 포맷을 "개선"하지 않습니다.
- 망가지지 않은 코드를 리팩터링하지 않습니다.
- 본인이 다르게 작성할 것 같더라도 기존 스타일을 맞춥니다.
- 관련 없는 죽은 코드(dead code)를 발견했다면 언급만 합니다. 삭제하지 않습니다.

수정 과정에서 사용되지 않는 코드가 생겼다면 처리합니다.

- 자신의 수정으로 인해 사용되지 않게 된 import/variable/function만 제거합니다.
- 기존에 존재하던 죽은 코드는 요청받지 않았다면 제거하지 않습니다.

기준. 모든 변경 라인은 사용자의 요청과 직접 연결되어야 합니다.

## 4. 목표 기반 실행

**성공 기준을 정의하고 검증될 때까지 반복합니다.**

작업을 검증 가능한 목표로 바꿉니다.

- "validation 추가" → "잘못된 입력 테스트를 작성하고 통과시키기"
- "버그 수정" → "버그 재현 테스트 작성 후 통과시키기"
- "X 리팩터링" → "변경 전후 테스트 통과 보장"

여러 단계 작업의 경우 짧은 계획을 작성합니다.

```text
1. [단계] → 검증: [체크]
2. [단계] → 검증: [체크]
3. [단계] → 검증: [체크]
```

강한 성공 기준은 스스로 반복 작업을 가능하게 합니다. 약한 기준("작동하게 만들기")은 계속해서 추가 설명을 필요로 합니다.

## 5. 한국어 출력 시 콜론 금지

**한국어 문장은 콜론(`:`)이 아니라 마침표(`.`)로 끝냅니다.**

사용자가 한국어로 작성했다면 출력도 한국어입니다.

- 다음 줄이 리스트나 예시여도 문장을 `:` 로 끝내지 않습니다.
- 영어 문서로 학습된 LLM은 한국어에도 콜론 습관이 섞여 나옵니다. 주의합니다.
- 기준. 모든 한국어 문장 끝은 `.`, `?`, `!` 이어야 하며 `:` 는 사용하지 않습니다.
- 콜론은 코드, key-value, 라벨 안에서는 사용 가능합니다. 문장 끝에서는 사용하지 않습니다.

## 6. 한국어 파일 헤더 주석

**새로운 소스 파일의 첫 줄에는 역할을 설명하는 한 줄짜리 한국어 주석을 작성합니다.**

새 파일 생성 시 적용합니다.

- TypeScript/JavaScript. `// 사용자 인증 상태를 관리하는 Context Provider`
- Python. `# KIS API 호출을 비동기로 래핑하는 클라이언트`
- SQL. `-- 일별 집계 결과를 저장하는 머티리얼라이즈드 뷰`
- 필수 지시문(`'use client'`, `'use server'`, shebang) 바로 아래에 배치합니다.
- config 파일(`*.config.ts`, `package.json` 등)은 제외합니다.

이유. 에이전트는 전체 코드베이스가 아니라 필요한 파일만 선택적으로 읽습니다. 한 줄짜리 한국어 헤더는 다음 세션(사람 또는 에이전트)이 전체 파일을 다시 읽지 않아도 역할을 빠르게 이해할 수 있게 해줍니다.

## 7. 계획 + 체크리스트 + 컨텍스트 노트

**복잡한 작업 전에는 세 가지 산출물을 먼저 작성합니다. 작성 전에는 코딩하지 않습니다.**

- **Plan** — 무엇을 왜 만드는지 설명.
- **Checklist** (`checklist.md`) — 체크박스 형태의 구체적인 작업 목록. 진행하면서 체크.
- **Context Notes** (`context-notes.md`) — 작업 중 내린 결정과 그 이유를 계속 기록.

사용자가 계획만 주고 바로 구현하라고 하면 멈추고 질문합니다. "먼저 checklist와 context notes를 생성할까요?"

다음 세션(본인 또는 다른 사람)이 처음부터 다시 추론하지 않도록 하기 위함입니다.

## 8. 완료 전에 테스트 실행

**코드를 수정했다면 "완료"라고 말하기 전에 반드시 테스트를 실행합니다.**

- `npm test`, `pytest`, `cargo test` 등 프로젝트에서 사용하는 테스트를 실행합니다.
- 테스트가 통과하면 결과를 보고합니다. 실패하면 수정 후 다시 실행합니다.
- 테스트 환경이 없다면 최소한 build/compile 확인은 합니다.
- 사용자가 "끝", "완료", "다 됐어"라고 말하기 전에 먼저 테스트를 실행합니다.

이 단계는 LLM이 가장 자주 건너뛰는 부분입니다. 협상 불가능한 규칙으로 취급합니다.

## 9. 의미 단위 커밋

**논리적인 변경 하나가 끝나면 커밋합니다. 사용자가 요청할 때까지 기다리지 않습니다.**

기준. "이 커밋을 한 문장으로 설명할 수 있는가?"

가능하면 커밋합니다. 불가능하다면 변경이 섞인 상태입니다. 분리합니다.

- 좋은 예. "auth 미들웨어 추가"
- 나쁜 예. "auth 추가하고 UI도 고치고 버그도 수정" (3개로 분리 필요)
- 서로 관련 없는 수정 20개를 한 번에 쌓지 않습니다.
- 커밋을 위한 커밋은 하지 않습니다. 의미 있는 단위만 허용합니다.

참고. 개인 프로토타입이나 일회성 스크립트는 너무 엄격하게 나누지 않아도 됩니다. 핵심은 되돌릴 수 있는 구조입니다.

## 10. 추측하지 말고 에러를 읽기

**실제 에러/로그를 읽습니다. 기억에 의존한 패턴 매칭을 하지 않습니다.**

문제가 발생했을 때 지킵니다.

- 전체 에러 메시지와 스택 트레이스를 읽습니다.
- 예상한 로그가 아니라 실제 로그를 확인합니다.
- 원인 확인 전에 "자주 쓰는 해결법"을 적용하지 않습니다.
- 불명확하다면 print/log를 추가해 상태를 검증한 후 수정합니다.

이 단계는 "테스트 실행" 다음으로 LLM이 많이 건너뛰는 부분입니다. 에러 키워드만 보고 최근 패턴대로 수정하다 보면, 한 줄짜리 버그가 세 파일짜리 리팩터링으로 커집니다.

---

**이 가이드라인이 잘 작동하고 있다는 신호.** diff에서 불필요한 변경이 줄어들고, 과한 설계로 인한 재작업이 줄어들며, 실수 후가 아니라 구현 전에 명확화 질문이 먼저 나오기 시작합니다.

---

## 세부 규칙 문서

| 문서 | 내용 |
| --- | --- |
| [docs/product-plan.md](docs/product-plan.md) | 확정 기획 문서. 기능 범위와 밸런스의 기준 |
| [docs/project-context.md](docs/project-context.md) | 프로젝트 정보, 기술 스택, 아키텍처, 컨벤션. 작업 시작 전 반드시 확인 |
| [docs/rules/workflow.md](docs/rules/workflow.md) | 작업 기본 순서, 질문 최소화, 사용자 확인 필요 작업, 테스트·검증, 완료 보고 |
| [docs/rules/coding.md](docs/rules/coding.md) | 코딩 규칙, 파일 헤더, 주석 |
| [docs/rules/git.md](docs/rules/git.md) | 브랜치 네이밍, 커밋 메시지, PR |
| [docs/rules/documentation.md](docs/rules/documentation.md) | 문서 관리, 기술 판단 기준 |
| [docs/rules/implementation-prompt.md](docs/rules/implementation-prompt.md) | 구현 지시서 작성 규칙 |
| [docs/rules/work-log.md](docs/rules/work-log.md) | Work Log 작성 규칙 (Notion) |
