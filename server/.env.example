# 이 파일을 복사해 server/.env로 만들고 값을 채운다.
# 루트 docker-compose.yml의 Postgres를 그대로 쓴다면 아래 값과 맞춰야 한다.
DATABASE_URL="postgresql://siniham:change_me@localhost:5432/siniham?schema=public"
JWT_SECRET="change_me_to_a_long_random_string"
PORT=3000
