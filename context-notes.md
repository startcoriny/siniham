# Context Notes

## 2026-08-15 (계속) - localStorage 목업을 실제 API로 재전환

사용자가 "왜 로컬스토리지로 해? 도커 켜놨는데?"라고 지적. 정원 리뉴얼 작업을 프로토타이핑하려고
8/8에 클라이언트를 다시 localStorage 목업으로 되돌려뒀는데(서버는 새 정원 계약에 맞춰 계속 동기화해둠),
Docker가 이미 떠 있는 지금은 그대로 둘 이유가 없어서 8/2에 했던 것과 같은 방식으로 실제 API로 되돌렸다.

`http.ts`(쿠키 인증 fetch 헬퍼)를 복원하고 `api.ts`/`gameApi.ts`가 `mockBackend.ts` 대신 실제
`/api/*`를 부르도록 바꿨다. 정원 리뉴얼로 새로 생긴 씨앗 구매(`/garden/seeds/purchase`)와 수확물
먹기(`/garden/produce/eat`), 심기 시 `cropId` 바디 파라미터도 서버 라우트와 맞춰 새로 추가했다
(예전 mockBackend 이전 버전의 `gameApi.ts`에는 없던 부분). `mockBackend.ts`는 더 이상 참조하는
곳이 없어 삭제했다.

전환 중 발견한 문제. `docker-compose.yml`의 server 컨테이너는 시작 시 한 번만
`prisma migrate deploy`를 실행하는데, 정원 리뉴얼 마이그레이션(`20260815160000_garden_renewal`)이
컨테이너 기동 이후에 추가돼서 DB에 반영이 안 되어 있었다. 실제 회원가입을 curl로 시도하니
`User.seedInventory` 컬럼이 없다는 500 에러가 나서 발견했다. `docker exec siniham-server-1
sh -c "cd server && npx prisma migrate deploy"`로 수동 적용해 해결. 컨테이너를 새로 띄우지 않고
새 마이그레이션만 추가했다면 이 명령을 다시 실행해야 한다는 걸 기억해둘 것.

localStorage 계정(예. 지민/1234)은 브라우저 저장소에만 있던 데이터라 이번 전환으로 이어지지 않는다.
실제 Postgres 기준으로 다시 회원가입해야 한다.

## 2026-08-15 - 정원 탭 전면 리뉴얼

정원은 기존 4개 카드와 단일 씨앗 구조에서 4라인 x 4슬롯의 넓은 텃밭으로 변경했다. 작물은 당근, 딸기, 토마토, 해바라기 4종이며 씨앗 가격, 성장 시간, 수확량, 먹기 효과를 공유 작물 마스터에서 관리한다.

클라이언트는 현재 실제 API가 아니라 localStorage 목업 어댑터를 사용하므로, 신규 플레이 흐름을 목업에 먼저 완성하고 Prisma와 Express API도 같은 데이터 계약으로 동기화했다. 이전 저장 상태의 단일 `seedCount`는 당근 씨앗으로 자동 이전한다. 서버 DB 마이그레이션도 기존 `seedCount`를 `seedInventory.CARROT`으로 옮긴다.

수확은 더 이상 즉시 재화를 지급하지 않고 작물별 수확물을 바구니에 저장한다. 사용자가 수확물을 선택해 햄스터에게 먹이면 배고픔과 작물별 기분 효과가 적용된다. 잡초 제거 보상은 씨앗 복제 대신 재화 1로 변경했다.

## 2026-08-04 - 포즈가 바뀔 때 햄스터가 작아지던 문제 (스프라이트 보정값 기준 변경)

사용자 신고. "물주기를 누르면 물마시러 가면서 점점 작아지고, 물 마시는 모습이 제일 작고,
그 뒤 가만히 있을 때도 작았다가, 다시 걸으면 원래대로 돌아온다."

원인은 `normalize-hamster-sprites.py`의 `SCALE_OVERRIDES`였다. 정규화는 **소품까지 포함한 전체
불투명 면적**의 제곱근으로 크기를 맞추는데, 그 위에 손으로 넣은 보정값이 포즈마다 제각각이었다.
특히 걷기를 0.90으로 낮춰뒀는데, 이건 "옆모습이라 실루엣 폭이 정면보다 23% 넓어 보인다"는 걸
줄이려던 것이었다. 폭은 맞춰졌지만 **몸통 면적이 정면 포즈보다 27% 작아졌다.**

162px로 렌더링했을 때 몸통 면적(소품 제외) 실측값.

| 포즈 | 수정 전 | 수정 후 |
| --- | --- | --- |
| idle / eat / wash / cheek / look | ~7,700 | 그대로 |
| walk | 5,620 (-27%) | 7,650 |
| sleep | 6,164 (-20%) | 7,770 |
| drink | 6,946 (-10%) | 7,724 |

`walk 0.90 -> 1.06`, `sleep 0.94 -> 1.06`, `drink 1.10 -> 1.16`으로 바꿨다. 소품이 없는 포즈는
전부 1.06으로 수렴한다(정규화가 이미 면적을 맞추므로). 소품이 있는 drink만 그 면적을 상쇄하려고 더 크다.

**기준을 폭이 아니라 몸통 면적으로 바꾼 것이 핵심이다.** 네발로 걷는 옆모습이나 누운 자세가 앉은
정면보다 길고 낮은 건 자연스럽다. 폭을 억지로 맞추면 몸집이 달라져서 "작아졌다"로 읽힌다.

그레이 `drink1.png`만 물병 없이 햄스터만 그려져 있어 `drink` 보정(1.16)을 받으면 20% 커진다.
`override_for()`가 사전 순서가 아니라 **가장 긴(구체적인) 접두어**를 고르도록 바꾸고 `drink1: 1.06`을
예외로 넣었다. 예전 방식은 "drink"가 "drink1"을 먼저 삼켰다.

재작업은 정규화 **이전 원본**(git `b6e03c0`)을 꺼내 한 번만 리샘플링했다. 이미 축소된 파일을 다시
키우면 화질이 누적으로 상한다.

진단 과정에서 배운 것. 케이지 화면에서 색으로 햄스터를 분리하려 하면 바닥(탄색)·나무집·밥그릇이
전부 주황 계열이라 측정이 오염된다. **스테이지 배경과 가구를 `visibility: hidden`으로 감추고 배경을
마젠타로 깔면** 햄스터만 정확히 잴 수 있다(`scratchpad/isolate-measure.js` 방식). DOM 상자 크기
(`getBoundingClientRect`)는 항상 162px로 일정해서 아무 단서가 안 됐다 - 크기 변화는 전부 그림 안에 있었다.

돌아간 길. 처음엔 "물 마실 때만 작다"고 보고 `drink.png` 하나만 1.2배로 키웠는데, 그러면 물 마시는
동안만 20% 커졌다가 끝나면 튀어서 오히려 더 나빠졌다. 되돌렸다. 한 포즈만 보지 말고 전 포즈의
몸통 면적을 한 번에 재는 게 맞다.

## 2026-08-03 - `docker compose up` 한 번으로 DB+서버+프론트 전부 기동

기존엔 DB만 도커였고 서버(`npm run dev:server`)와 프론트(`npm run dev:client`)는 매번 따로 켜야 했다.
`docker-compose.yml`에 `server`, `client` 서비스를 추가해 한 번에 뜨게 했다.

- 이미지는 별도 Dockerfile 없이 `node:24-alpine`을 그대로 쓰고, 컨테이너 시작 커맨드에서 매번
  `npm install` -> (server만) `prisma generate` + `migrate deploy` -> `npm run dev -w <workspace>` 순으로 실행한다.
  스키마가 바뀌어도 컨테이너를 다시 띄우기만 하면 최신 상태로 맞춰진다.
- `node_modules`는 호스트(Windows)와 절대 안 섞이게 컨테이너별 익명 볼륨으로 분리했다
  (`server-root-modules`, `client-client-modules` 등 6개). 호스트의 `node_modules`는 Windows용
  네이티브 바이너리(esbuild, prisma engine)라 리눅스 컨테이너 안에서 그대로 bind mount하면 깨진다.
- vite의 `/api` 프록시 대상이 문제였다. client 컨테이너 안에서 `localhost:3000`은 client 컨테이너
  자신이라 server 컨테이너로 못 간다. `vite.config.ts`가 `VITE_API_PROXY_TARGET` 환경변수를 읽게
  바꾸고, docker-compose의 client 서비스에서만 `http://server:3000`으로 넘긴다. 호스트에서 그냥
  `npm run dev:client`로 띄울 땐 이 값이 없어서 기존처럼 `localhost:3000` 그대로 쓴다 - 두 방식이
  공존한다.
- db 서비스에 healthcheck를 추가해 server가 `service_healthy`를 기다리게 했다(기존엔 db에
  healthcheck가 없어서 prod compose에만 있었다).
- 실제로 `docker compose up -d`로 띄워서 `curl localhost:5173/api/health`가 client의 프록시를
  거쳐 server까지 갔다 오는 것까지 확인했다. `server listening on :3000`, `VITE ready`,
  `No pending migrations to apply` 전부 정상.

## 2026-08-02 - 햄스터 클릭으로 쓰다듬기

케이지의 햄스터를 클릭하면 쓰다듬어진다. 원래 클릭은 상세 정보 모달을 여는 동작이었는데,
상태 패널에 "자세히 보기" 버튼이 이미 있어서 클릭을 쓰다듬기로 넘겼다.

연출은 세 가지를 겹쳤다. `pet.png`에 사람 손이 그려져 있어 쓰다듬는 그림이 그대로 나오고,
몸이 살짝 눌렸다 돌아오며(`animate-pet-squish`), 머리 위로 하트가 떠오른다.
연달아 누르면 토스트가 계속 쌓여서 성공 토스트는 생략하고(`act`에 `silent` 옵션) 오류만 알린다.

CSS 애니메이션 재생에 두 가지 함정이 있었다.

- 처음엔 래퍼 div에 `key`를 줘서 다시 재생시켰는데, 그러면 `HamsterSprite`까지 리마운트돼
  깜박임 타이머와 걷기 프레임 상태가 초기화된다. 클래스를 뗐다 붙이는 방식(`setSquishing(false)` ->
  `requestAnimationFrame`으로 `true`)으로 바꿨다.
- 눌리는 연출을 버튼에 직접 걸면 위치 이동용 `-translate-x-1/2 -translate-y-1/2`와 충돌한다.
  안쪽에 div를 하나 더 두고 거기에 걸었다. 기준점은 `bottom center`라 바닥에 붙은 채 눌린다.

`prefers-reduced-motion`이면 두 애니메이션 모두 끈다.

설계서와 달라진 점. 원래는 "쓰다듬기 모드를 켠 뒤 클릭", 그냥 클릭은 쳐다보기 등 가벼운 반응이었다.
클릭 = 쓰다듬기로 단순화했고 screen-design.md에 반영했다. 가벼운 반응(말풍선, 가까이 다가오기)은 아직 없다.

알아둘 것. `/api/hamster/action`의 PET에는 서버 쿨다운이 없다. 클라이언트가 약 1.2초 동안 다음 동작을
막지만 API를 직접 부르면 제한이 없다. 친밀도와 기분은 100에서 잘리고 미션도 목표에서 멈추므로 큰 문제는
아니지만, 밸런스를 정할 때 같이 볼 항목이다.

## 2026-08-02 - 케이지 연출 다듬기 (모래목욕통 크기 / 집 앞에서 자기 / 걷기-먹기 크기)

사용자 피드백 세 가지를 처리했다.

모래목욕통은 통 앞에 서서 세수하던 것을 통 안 모래 위에 올라앉는 걸로 바꿨다. 사용자가 참고 이미지를
줬는데 햄스터가 모래 위에 앉고 몸 전체가 보이는 구도였다. 처음엔 통을 햄스터보다 앞에 그려(z-index)
파묻히게 만들었는데 반대였다. 기본 순서(햄스터가 앞) 그대로 두고 발이 모래면에 닿도록 `offsetY`를
-0.19로 올리는 게 맞았다. 비율도 안 맞아서 표시 크기를 `w-20 md:w-32`에서 `w-40 md:w-64`로 키웠다.
참고 이미지의 햄스터 몸 폭이 통 폭의 약 3분의 1이라 그 비율에 맞췄다.

집에서 자는 연출은 햄스터를 감추고 `Z z z` 말풍선을 띄우는 방식이었는데, 그냥 집 앞에서 자는 걸로 바꿨다.
`isInHouse`, `houseActivityPosition` 상태와 말풍선을 전부 지우고 다른 가구와 같은 경로로 통일했다.
`sleep.png`에 이미 z 표시가 그려져 있어 말풍선이 필요 없다. 가구별로 설 자리를 정하는 `offsetX`/`offsetY`를
추가했고, 집은 보통 위쪽 벽 쪽에 놓여 있어 아래로 0.16 내려 잡는다. 산책 범위(y 0.5~0.78)를 그대로 쓰면
집까지 못 올라가서 가구 행동은 y 0.35까지 허용한다.

"밥 앞으로 가면 작아진다"는 정규화 때문이었다. 걷기는 옆모습이라 몸이 길게 퍼지고 먹기/서있기는 정면이라
뭉쳐 보인다. 면적을 같게 맞췄더니 실루엣 폭이 351 대 272로 23% 차이나서 걷다 멈추면 작아진 것처럼 보였다.
`SCALE_OVERRIDES`에 걷기 0.90, 정면 자세(idle/eat/wash/cheek/look) 1.06을 넣어 양쪽을 가운데로 당겼다.
폭 차이가 10%로 줄어 전환이 눈에 띄지 않는다.

### 이어서 - 케이지 스테이지 반응형 정리

위 작업 중에 화면 크기에 따라 비율이 달라지는 문제를 발견해 이어서 고쳤다. 햄스터 스프라이트는
`size={160}` 고정인데 가구는 반응형 px 폭(`w-40 md:w-64`)이라 모바일에서는 햄스터만 커 보였다.
가구별 `offsetY`도 비율 좌표라 브레이크포인트마다 어긋났다.

전부 스테이지 폭 대비 비율로 바꿨다. `ResizeObserver`로 스테이지 실제 폭을 재고 `ITEM_ASSET.widthRatio`,
`HAMSTER_WIDTH_RATIO`를 곱해 px를 만든다. 첫 페인트에 큰 크기로 그렸다가 줄어드는 깜빡임이 없도록
`useLayoutEffect`에서 잰다. 비율값은 기존 PC 화면(스테이지 736px)에서 역산해 데스크톱 모습은 그대로 뒀다.

두 가지를 같이 고쳤다.

- 스테이지의 `min-h-72`를 제거했다. 좁은 화면에서 10:7 비율이 깨져 위치 계산과 어긋났다.
- 상태/스테이지 좌우 분할을 `md:grid-cols-[240px_1fr]`에서 `lg:grid-cols-[220px_1fr]`로 옮겼다.
  md(768px)에서 나누면 좌측 메뉴까지 겹쳐 820px 화면에서 스테이지가 276px까지 줄었다. 지금은 532px다.

측정 결과. PC 756px / 태블릿 532px / 모바일 334px 모두 비율 1.43이고, 스테이지 폭 대비 햄스터 0.213~0.214,
집 0.191~0.194, 모래목욕통 0.340~0.344로 일치한다.

이 과정에서 `gen-hamster-blink.py`의 눈 좌표가 하드코딩이라 정규화 배율을 바꾸면 조용히 어긋난다는 걸
발견했다. 어두운 픽셀 덩어리 중 크기와 높이가 비슷한 좌우 한 쌍을 찾는 방식으로 바꿔 배율이 바뀌어도
따라오게 했다. 몸 외곽선(너무 큼), 귀 선(너무 길쭉), 머리 선(너무 납작)은 크기와 가로세로비로 걸러낸다.

## 2026-08-02 - 대기 상태 눈 깜박임

템포를 늦추면서 가만히 서 있는 구간이 길어졌는데 정지 그림이라 멈춘 것처럼 보였다. 눈 깜박임을 넣어 덜었다.

에셋. 눈 감은 그림이 없어서 `scripts/gen-hamster-blink.py`로 `idle.png`에서 만들었다. 눈 영역을
위아래 색으로 세로 보간해 지우고(위는 털색, 아래는 크림색 주둥이라 단색으로 덮으면 티가 난다) 그 자리에
가운데가 살짝 처진 눈꺼풀 선을 그린다. 눈 위치는 어두운 픽셀 덩어리를 훑어 좌우 대칭인 쌍으로 찾아
스크립트에 좌표로 박아뒀다(골든/그레이 각 2개).

주의했던 것 두 가지. 주변 색을 눈 바로 옆에서 뽑으면 테두리에 걸려 회색 띠가 남는다(6px 떨어뜨려 해결).
눈에서 가장 어두운 픽셀은 테두리의 순검정이라 눈꺼풀 색으로 쓰면 너무 세다(가장 흔한 진한 색으로 변경).

재생. 깜박임은 "오래 뜨고 아주 잠깐 감는" 리듬이라 일정 간격 프레임 배열로 두면 낭비가 크다.
감은 그림 한 장만 만들고 `HamsterSprite`가 1.8~4.8초마다 140ms 동안 바꿔치기한다. 20% 확률로 두 번
연달아 깜박이되 세 번 이상은 막았다(눈을 떠는 것처럼 보인다). 행동이 `IDLE`일 때만 동작한다.

간격을 처음엔 2.6~6초로 뒀는데, 케이지에서 가만히 서 있는 구간이 4~9초라 절반 정도는 깜박임을 못 보고
지나갔다. 1.8~4.8초로 줄여 40초에 6회 정도 보이게 맞췄다.

`blink.png`는 어떤 행동 접두어와도 겹치지 않아 `getHamsterSpriteVariants`에 걸리지 않는다.
파일명을 `idle-blink.png` 같은 식으로 두면 정렬상 `idle.png`보다 앞서 기본 그림이 뒤바뀐다.

`<img>`의 `key={src}`도 제거했다. 그림이 바뀔 때마다 img가 새로 마운트돼 깜박임과 걷기 프레임에서
한 프레임씩 깜빡였다.

## 2026-08-02 - 햄스터 스프라이트 규격 정규화

사용자가 "걷는 이미지, 서 있는 이미지, 밥 먹는 이미지 크기가 왜 다 다르냐"고 지적했다. 코드가 아니라
에셋 문제였다. `HamsterSprite`는 모든 행동을 같은 정사각 상자에 그리는데 원본이 제각각이었다.

- 캔버스 비율이 달랐다. 대부분 1536x1024(3:2), 걷기 프레임만 512x512(1:1), 그레이 idle은 1024x1024.
  `<img width height>`로 정사각을 강제하니 3:2 그림만 가로로 1.5배 눌렸다.
- 캔버스 안에서 햄스터가 차지하는 비율도 달랐다. idle 0.31x0.51, 걷기 프레임 0.69x0.55, pet 0.46x0.79.

둘이 겹쳐 화면상 크기가 2배 넘게 차이났다(idle 가로 50px 대 걷기 110px).

`scripts/normalize-hamster-sprites.py`로 전부 다시 저장했다. 알파로 잘라내고, **불투명 면적의 제곱근**을
기준으로 크기를 맞춘 뒤 512x512 캔버스에 가로 중앙 + 바닥 정렬한다. 가로나 세로 하나만 기준으로 삼으면
걷는 자세처럼 납작한 그림이 과하게 커지거나 작아지는데, 면적은 자세가 바뀌어도 몸집이 일정해서 안정적이다.
바닥 정렬은 걷다가 멈춰도 발 위치가 튀지 않게 하려는 것이다.

소품이 함께 그려진 파일(pet의 사람 손, wheel의 쳇바퀴, drink의 물병)은 면적이 부풀려져 햄스터가 작아지므로
`SCALE_OVERRIDES`에 파일별 보정값을 뒀다.

두 가지 함정이 있었다.

- `walk2.png`는 배경 제거 후 남은 옅은 픽셀(알파 40~249)이 캔버스 전체에 흩어져 있어 단순 `getbbox()`가
  1013x1024를 반환했다. 행/열별 픽셀 수를 세서 일정 수 이상인 구간만 내용으로 치도록 바꿔 해결했다.
- `walk-01/sheet-source.png`는 프레임을 잘라낸 원본 시트라 정규화 대상이 아니다. `SKIP_FILES`로 제외했다.

부수 효과로 용량이 크게 줄었다. 160px로 표시하는 스프라이트가 장당 2.2MB짜리 1536x1024 PNG였다.
`assets/hamsters` 69MB -> 7.9MB, 개별 파일은 2.2MB -> 115~152KB.

재실행 주의. 원본을 덮어쓰면 리샘플링이 누적되므로 스크립트는 원본 사본을 입력으로 받는다.
원본은 git 이력에 남아 있다.

## 2026-08-02 - 케이지 자율 행동 템포 조정

사용자가 "행동이 너무 빠르다, 한 번 걸을 때 한 행동이 아니라 걷는 걸 여러 번 해도 된다"고 해서
케이지 연출 템포를 전면 조정했다.

이전 상태. 대기 0.5~1.4초 -> 걷기(거리와 무관하게 850ms 고정) -> **74% 확률로** 가구 행동 2~3초.
가구 5종을 다 가지면 확률이 16+16+14+14+14로 74%가 돼서 걸을 때마다 거의 매번 뭔가를 하러 갔다.
쉬는 구간이 사실상 없고, 이동 시간이 고정이라 멀리 갈수록 빨리 달리는 것처럼 보였다.

바꾼 것.

- 가구 행동 확률을 각 5%로 낮춤 (5종을 다 가져도 25%)
- `ACTIVITY_MIN_GAP` - 가구 행동 뒤에는 최소 2번의 걷기/쉬기가 지나야 다시 간다. 확률만 낮추면
  운 나쁘게 연속으로 나올 수 있어서 별도 장치를 뒀다
- 동작 사이 대기를 2~5초로 늘림
- 걷기를 거리 비례 시간으로 계산(`WALK_SPEED_PER_MS`)해 속도를 일정하게 하고, 그 속도 자체도 늦춤
- 목적지를 케이지 전체 랜덤에서 현재 위치 근처(`wanderTarget`)로 바꿔 종종걸음 대신 산책처럼
- 30% 확률로 걷지 않고 제자리에서 쉬는 구간 추가
- 걷기 프레임 간격을 200ms -> 360ms로 늘려 느려진 이동 속도와 맞춤(쳇바퀴는 160ms 유지)
- 돌보기 액션 후 가운데로 순간이동하던 것을 제거. 있던 자리에 두면 이어지는 자율 행동이 자연스럽다

조정 중에 알게 된 것. 쉬기가 연달아 나오면 16초 넘게 완전히 멈춰 있어 고장 난 것처럼 보였다.
정지 스프라이트라 변화가 아예 없기 때문이다. 연속 쉬기를 한 번으로 제한해 멈춤 구간을 최대 9초로 줄였다.
나중에 대기 상태용 프레임(숨쉬기 정도)이 생기면 이 제한을 풀어도 된다.

측정 결과(75초 관찰). 걷기 1.3~2.9초, 서 있기 3.8~9.0초, 가구 행동은 32초 간격으로 2회.
서버 자율 행동 쿨다운이 20초라 이전에는 대부분의 행동이 쿨다운에 막혀 수치가 반영되지 않았는데,
템포를 늦추니 간격이 쿨다운을 넘겨 실제로 청결/기분이 오른다.

## 2026-08-02 - 터널 교체와 세수하기 명칭 정리

사용자가 터널을 지적해서 확인해 보니 터널은 아예 동작하지 않는 아이템이었다. 상점 최고가(150재화)인데
`ITEM_ASSET`에 이미지 항목이 없어 사면 케이지에 아무것도 안 보였고, 연결된 행동도 상태 효과도 없었다.
설계서에는 "안으로 들어가 논다"와 가구 애니메이션 3종이 적혀 있었지만 전부 미구현이었다.

기존 행동표에 이미 있는데 트리거가 없던 행동에 가구를 붙이는 방향으로 3종을 새로 만들었다.
모래목욕통(세수하기), 해바라기씨 접시(볼에 저장), 전망대(서서 둘러보기). 전부 120재화다.
이 중 "볼에 저장"과 "서서 둘러보기"는 도감에 있으면서 발견할 방법이 전혀 없던 행동이다.

자율 행동은 화면 연출만 있고 서버에 남지 않던 상태였다. `POST /api/hamster/idle-activity`를 만들어
가구 보유 확인 -> 수치 반영 -> 행동 도감 등록까지 처리한다. 회복량은 사용자가 버튼으로 시키는
돌보기(+20~30)보다 작게(+5~8) 두고, 화면에 머무는 것만으로 수치가 계속 오르지 않도록 `Hamster.lastIdleAt`
기준 20초 쿨다운을 건다. 쿨다운 중이면 오류 대신 현재 상태를 그대로 돌려준다(연출은 이미 끝났으므로).
이 작업으로 쳇바퀴와 집 자율 행동도 처음으로 도감에 등록되게 됐다.

액션 버튼 "청소하기"는 실제로는 전부 세수하기였다. 재생되는 스프라이트가 `WASH`(앞발로 얼굴 씻기)이고
효과가 청결 +20으로 product-plan.md의 "세수하기 청결 +20"과 같았으며 도감에도 "세수하기"로 등록되고
있었다. 설계서 6.5의 "케이지 전체 청소" 연출은 에셋도 로직도 없이 이름만 남은 흔적이라 세수하기로
통일하고 API 액션 id도 `CLEAN` -> `WASH`로 바꿨다(DB에 저장되는 값이 아니라 요청 enum이라 안전).

발견한 버그. 상점에서 산 가구가 전부 기본 좌표(0.5, 0.5)에 생성돼 케이지 한가운데 겹쳐 쌓였다.
가구가 쳇바퀴 하나뿐일 때는 티가 안 났는데 3종을 추가하니 바로 드러났다. `pickFreeSlot()`으로
미리 정한 후보 자리 중 비어 있는 곳에 놓도록 고쳤다.

가구 이미지는 Python + PIL로 저해상도(약 110px)에 직접 그린 뒤 NEAREST 8배 확대해 기존 에셋의
픽셀 룩에 맞췄다. 생성 스크립트는 `scripts/gen-cage-items.py`에 남겨 재생성할 수 있게 했다.
기존 에셋(먹이통, 물그릇 등)만큼 음영이 풍부하지는 않다. 나중에 더 좋은 그림으로 교체할 수 있다.

이미지가 케이지에만 있고 상점 카드는 이름 텍스트 자리표시자였어서, `client/src/lib/cageItemAssets.ts`로
이미지 맵을 뽑아 두 곳이 같은 그림을 쓰게 했다.

## 2026-08-02 - 남은 작업 5건 마무리

체크리스트를 실제 코드와 대조해 남은 항목을 추려 전부 처리했다. 정원 오프라인 요약, 미션 탭 요약, 시작 화면 분리, 회원 탈퇴 API, 약관/개인정보 화면이다.

정원 소식 모달은 목업 시절의 고정 문구가 그대로 남아 있었다. 실제 데이터로 바꾸려다 보니 product-plan.md 오프라인 진행 항목에 있는 "잡초 생성"이 아예 구현돼 있지 않았다는 걸 발견했다. `hasWeed`가 항상 false라 잡초 제거 기능 자체가 도달 불가였다. `GardenPlot.weedFrom`을 기준으로 5분마다 잡초가 생기도록 구현하고, 심기와 잡초 제거 시 기준 시각을 다시 잡는다. 확률이 아니라 결정론적 계산이라 테스트로 검증할 수 있다.

요약은 조회 시점의 변화를 그때그때 보여주는 대신 `User.pendingGrownCount`/`pendingWeedCount`에 누적해둔다. 화면을 보고 있는 동안 밭이 익었다고 모달이 뜨면 흐름이 끊기므로, 마지막 접속에서 30분(수면 전환 기준과 동일) 이상 지난 조회에서만 누적한다. 확인은 `POST /api/garden/summary/ack`로 서버에 남기고, 기존 localStorage 하루 1회 게이트는 없앴다.

이 과정에서 로그인 라우트가 `lastActiveAt`을 즉시 갱신하고 있던 걸 발견했다. 그 탓에 로그인 직후 첫 `/api/state`가 오프라인 간격을 0으로 보고, 정원 소식뿐 아니라 햄스터 배고픔/목마름 감소와 수면 전환까지 반영하지 못했다. 갱신은 `tickHamsterState`에만 맡기도록 제거했다.

자정까지 남은 시간은 클라이언트가 따로 계산하면 서버의 일일 리셋 기준(Asia/Seoul `missionDate`)과 어긋날 수 있어서, 서버가 `missionResetInMs`로 내려주고 화면은 경과분만 빼서 표시한다.

시작 화면을 분리하면서 로그인/회원가입을 각각 별도 경로로 나눴다(`/`, `/login`, `/signup`). 로그인 성공 시 분기는 GameShell이 이미 햄스터 유무로 `/onboarding` 리다이렉트를 하고 있어서 `/home`으로만 보내면 된다.

이용약관과 개인정보처리방침은 실제 저장 항목만 적은 초안이다. 두 화면 모두 상단에 "출시 전 초안, 법률 검토 필요" 안내를 고정으로 넣어뒀다.

남은 제품 결정. 정원 성장 10분과 잡초 5분은 product-plan.md에 수치가 없어 정한 임시값이다. 코드로는 더 진행할 수 없고 밸런스가 정해지면 `GROW_DURATION_MS`, `WEED_INTERVAL_MS` 두 상수만 바꾸면 된다.

## 2026-08-02 - 로컬 개발 환경 재구성

작업을 이어받았을 때 `node_modules`와 DB 컨테이너, 볼륨이 모두 없어진 상태였다. 5432 포트는 다른 프로젝트의 Postgres가 쓰고 있어서 이 프로젝트는 5433으로 띄우기로 하고 `.env`의 `POSTGRES_PORT`와 `DATABASE_URL`, `server/.env`를 그 값으로 맞췄다. 볼륨이 없어졌으므로 기존 `tester` 계정을 포함한 DB 데이터는 남아 있지 않다.

Prisma CLI는 `npx prisma`가 PATH를 못 잡아서 실패한다. 루트에서 `node_modules\.bin\prisma.cmd <명령> --config server\prisma.config.ts` 형태로 직접 부르면 동작한다.

## 2026-08-01 (계속) - 화면 우선 전략으로 전환

Docker Desktop 설치는 했지만 WSL2가 안 깔려 있어서 DB 연결이 재부팅 전까지 막힘. 사용자 결정으로 전략을 바꿈. 인증/데이터를 전부 프론트 목업 + localStorage로 채우고 [docs/specs/screen-design.md](docs/specs/screen-design.md) 기준 화면 전부(1~9단계)를 먼저 완성한 뒤, 나중에 10단계에서 실제 API로 순서대로 교체.

리스크 완화 장치. `shared/types/*`에 실제 API와 주고받을 타입을 미리 정의하고 목업 데이터가 그 타입을 그대로 따르게 해서, 나중에 "로컬 상태 -> API 호출" 교체 시 화면 컴포넌트는 거의 안 건드리게 함.

캐릭터 아트 한계. 설계서 8장의 실제 픽셀 아트 스프라이트(48x48, 7색, 약 59프레임)는 제가 그릴 수 없음. 준비될 때까지 자리표시자로 진행하고, 5단계(애니메이션)부터는 자리표시자의 한계가 드러날 것.

설계서 핵심 수치.

- 케이지 스테이지 논리 크기 1000x700, 가구/햄스터 위치는 절대 픽셀이 아니라 0~1 비율(posX/posY)로 저장 - PC/모바일 공용
- 스프라이트 작업 크기 48x48px, 화면 표시는 96/144/192px 정수배, `image-rendering: pixelated`
- 컬러 팔레트. 배경 #FFF4DD, 카드 #FFF9EB, 메인브라운 #5A3928, 포인트핑크 #F28C9A, 포인트그린 #77A96B, 포인트옐로 #F5C451, 위험 #D86464
- PC 콘텐츠 최대폭 1200px, 좌측메뉴 180~220px / 모바일 하단탭 60~72px, 터치 최소 44px
- 상태 저장은 localStorage로 임시 구현 후 서버로 교체 예정

## 2026-08-01 (계속) - 인증 방식을 닉네임으로 변경

사용자가 화면을 직접 보려고 시도하다 인증이 막혀있는 걸 발견. 인증 방식을 이메일에서 닉네임 + 비밀번호로 바꾸기로 결정하고, 목업 로그인 테스트 계정으로 `tester` / `1234`를 지정함.

- server/의 Prisma User 모델, 인증 라우트(signup/login/logout/me)는 email 기준으로 이미 작성돼 있음. 지금은 건드리지 않고, 10단계(실제 API 연결) 때 닉네임 기준으로 다시 맞추기로 함.
- `shared/types/auth.ts`(SignupRequest/LoginRequest/AuthUser, email 필드 포함)도 실제 백엔드 계약을 그대로 반영한 것이라 지금은 안 건드림. 프론트 목업은 별도로 `client/src/mocks/mockAuth.ts` + `AuthContext`에서 처리 - shared 타입과 완전히 분리됨.
- 기존 `client/src/lib/api.ts`(실제 API 호출 래퍼)는 더 이상 참조하는 곳이 없어서 삭제. 10단계에서 닉네임 기준으로 다시 작성.
- 세션은 `localStorage`에 닉네임만 저장. 새로고침해도 로그인 상태 유지, 로그아웃하면 제거.

## 2026-08-01 (계속) - 캐릭터 에셋 우선순위로 진행 순서 재조정

햄스터 스프라이트가 골든 3포즈만 있는 상태라, 캐릭터 의존도가 낮은 화면부터 먼저 만들기로 함. 새 순서. 공통 게임 레이아웃(3단계) -> 상점(8단계) -> 설정/계정(신규) -> 미션 탭(9단계 절반) -> 정원(7단계) -> 행동 도감 탭(9단계 절반). 온보딩·케이지·캐릭터 애니메이션은 캐릭터 에셋이 더 채워질 때까지 보류.

설정 및 계정 화면은 screen-design.md 2.3에 화면 9로 나와 있지만 11장 "화면 우선 개발 순서" 1~10단계 목록에는 빠져있었음 - 이번에 순서에 추가함.

## 2026-08-01 (계속) - 캐릭터 우선순위 6개 작업 완료

공통 게임 레이아웃, 상점, 설정/계정, 미션 탭, 정원, 행동 도감 탭까지 순서대로 전부 끝냄. `GameStateContext`가 재화/보유아이템/미션진행도/정원구획/씨앗/발견행동을 전부 들고 있고 전부 localStorage에 유지됨.

여러 화면에서 "실제 플레이로는 나올 수 없는 시연용 초기값"을 넣어뒀다(미션 진행도, 정원 4구획 상태, 발견된 행동 3개). 전부 checklist.md에 어디에 있는지 기록해뒀고, 10단계(실제 API 연결)나 케이지/온보딩(보류 그룹) 작업 시작 시 전부 0/미발견 상태로 정정 필요. 검색 키워드. "시연용".

목업 성장 타이머(`MOCK_GROW_DURATION_MS`, 10초)도 마찬가지로 실제 값이 아님 - product-plan.md 3장의 실제 lazy-tick 수치(배고픔 8시간, 목마름 6시간, 청결 24시간)로 10단계에서 교체.

행동 도감의 "테스트. 다음 미발견 행동 발견하기" 버튼은 케이지가 없어서 넣은 임시 트리거. 케이지 완성되면 실제 행동 실행 지점에서 `discoverBehavior()`를 직접 호출하도록 바꾸고 이 버튼은 삭제.

## 표기 관련

사용자 환경(Windows PowerShell)에서 유니코드 기호가 깨져 보인 적이 있어서(`wsl --status` 결과 한글이 깨짐), 이후 문서와 채팅 답변에서는 화살표(->) 등 특수기호 대신 일반 텍스트를 우선 사용.

## 2026-08-01 — 개발 순서 설계

풀스택을 기능 단위 수직 슬라이스로 진행하기로 결정. 슬라이스 안에서는 백엔드(모델+API) 먼저, 프론트 나중. 이유는 JWT 쿠키 인증과 lazy-tick 계산처럼 프론트가 흉내 내기 어려운 로직이 많아서, 목업을 만들었다가 실제 API에 맞춰 다시 고치는 비용을 피하기 위함.

슬라이스 순서. 인증 → 케이지 핵심 루프 → 정원 → 상점/가구 → 미션/도감 → 에셋 교체 → 배포. 케이지가 정원보다 먼저인 이유는 서비스 한 문장(케이지·햄스터 관찰)에 가장 가깝기 때문.

## 2026-08-01 — 개발 환경 구축

이 환경(Windows, VS Code 확장)에 Git, Node.js, Docker가 전부 설치되어 있지 않은 상태로 시작. Git과 Node.js는 winget으로 설치(가벼움, 재부팅 불필요). Docker Desktop은 WSL2 활성화·재부팅이 필요할 수 있어 사용자 확인 후 진행 여부 결정하기로 함.

- Git 2.55.0, Node.js v24.18.1(LTS) 설치 완료.
- Docker/PostgreSQL은 사용자가 "지금은 DB 없이 화면만" 선택. → 0~1단계는 DB 없이 코드와 화면까지만 만들고, 실제 회원가입/로그인 동작 검증은 DB 연결 이후로 미룸. 서버가 부팅되는지, 타입이 맞는지까지만 확인.

## Repository

- GitHub. https://github.com/startcoriny/siniham.git (연결 완료, main 브랜치)
- Notion Work Log 위치. `햄스터 키우기(시니햄) > 기록` (https://app.notion.com/p/3af9f20303a48062b290e3fc25c2ccf1)

## 결정 — 패키지 버전

package.json의 의존성 버전은 직접 하드코딩하지 않고 `npm install <pkg> -w <workspace>`로 설치해 npm이 현재 시점 최신 안정 버전을 resolve하도록 함. 모델의 기억에 의존한 버전 번호는 최신성이 보장되지 않기 때문.

## 알려진 이슈 — npm audit (react-router)

`react-router-dom` 7.18.2 설치 시 high severity 취약점 1건 발견. RSC(React Server Components) 모드의 CSRF 우회 이슈(GHSA-qwww-vcr4-c8h2). 이 프로젝트는 Vite SPA + 클라이언트 사이드 라우팅만 쓰고 RSC를 쓰지 않아 해당 사항 없음으로 판단해 지금은 그대로 둠. `npm audit fix --force`는 7.11.0으로 다운그레이드하는 breaking change라 임의로 적용하지 않음. 배포 전에 재확인 필요.

## 2026-08-01 — 0단계 스캐폴딩 + 1단계 인증 화면 구현 중 발견한 것

npm install로 최신 버전을 그대로 받았더니 기억(학습 시점) 기준과 다른 major 버전들이 깔렸다. 전부 실제 동작을 검증하며 맞춤.

- **Prisma 7.** 기본 generator가 `prisma-client`(ESM)로 바뀌었고 `output` 경로 명시가 필수. `datasource`에 `url = env(...)`를 안 쓰고 대신 `prisma.config.ts`에서 `datasource.url`로 읽음(`dotenv` 별도 설치 필요). 클라이언트 생성 방식도 바뀌어서 `new PrismaClient()` 단독 호출이 안 되고 `@prisma/adapter-pg`로 driver adapter를 넘겨야 함 (`new PrismaClient({ adapter: new PrismaPg({ connectionString }) })`). `npx prisma init`이 `.claude/skills`, `.windsurf/skills`, `.agents/skills`, `skills-lock.json`까지 같이 만들어주는데, 이 프로젝트는 Claude 단독 진행이라 전부 삭제함.
- **TypeScript 7.** `tsconfig.json`의 `baseUrl`이 제거됨 (`TS5102`). `baseUrl` 없이 `paths`만 써도 tsconfig 파일 위치 기준으로 정상 동작.
- **Tailwind v4.** `tailwind.config.js` + `postcss.config.js` 조합 대신 `@tailwindcss/vite` 플러그인 + `src/index.css`에 `@import "tailwindcss";` 한 줄로 충분. `postcss`/`autoprefixer`는 이제 불필요해서 설치했다가 제거함.
- **tsx의 tsconfig paths 해석.** `@shared/*` 별칭이 `tsc --noEmit`뿐 아니라 `tsx` 런타임에서도 별도 설정 없이 그대로 해석됨. 실제로 서버를 띄우고 `/api/auth/signup`을 호출해 확인.

## 결정 — 브라우저 검증 방법

이 환경(Windows)에는 `chromium-cli`가 없어서 run 스킬의 fallback대로 Playwright를 직접 사용. 단, 브라우저 바이너리를 새로 받지 않고 시스템에 이미 설치된 Chrome을 `channel: "chrome"`으로 구동해 다운로드 비용을 없앰. Playwright는 프로젝트 저장소가 아니라 OS 임시 스크래치패드에만 설치해서 레포에 흔적을 남기지 않음. 스크린샷으로 로그인/회원가입 두 모드 렌더링을 눈으로 확인함.

## 결정 — shared 폴더

`shared/`는 npm workspace 패키지로 만들지 않음(package.json 없음). client/server 양쪽 tsconfig에 `@shared/*` 경로 별칭만 걸어서 참조. 지금 실제로 공유할 내용(인증 요청/응답 타입)이 생겼을 때만 파일을 추가함 — 당장 쓰이지 않는 타입은 미리 만들지 않음.

## 2026-08-02 - 온보딩과 케이지 핵심 루프 완료

골든과 그레이 에셋이 충분히 준비되어 보류했던 온보딩과 케이지 작업을 진행했다. 사용자당 햄스터 한 마리를 갖는 `Hamster` 모델을 추가했고, 신규 사용자는 `/onboarding`에서 이름과 외형을 정한 뒤 케이지로 들어간다.

케이지에는 비율 좌표 기반 가구 배치, 상태바, 먹이/물/쓰다듬기/청소, 상세 정보, 꾸미기 모드를 구현했다. 돌보기 액션은 햄스터 상태 변경과 미션 진행, 행동 도감 발견을 서버 트랜잭션에서 함께 처리한다. 정원 심기도 정원 미션과 `GARDEN` 행동 발견에 연결했다.

오프라인 진행은 마지막 접속부터 최대 3일까지만 배고픔, 목마름, 청결, 기분을 낮추고 체력을 회복한다. 30분 이상 경과하면 수면 상태가 된다. 일일 미션 날짜는 `Asia/Seoul` 기준이다. 관련 순수 계산은 Node 내장 테스트로 검증한다.

운영 준비로 서버 번들 빌드, 앱과 DB Docker Compose, 마이그레이션 자동 적용, JSON 오류 처리, 환경변수 검증, DB 백업 스크립트와 GitHub Actions CI를 추가했다. 실제 서버에서 신규 가입 -> 햄스터 생성 -> 먹이 액션 -> 미션/도감 반영 -> 가구 이동 API 흐름을 확인했다.

## 2026-08-02 - 걷기 애니메이션 프레임 추가

기존 `walk`, `walk2`는 연속 프레임이 아니라 서로 다른 걷기 시안이다. 골든은 `walk2.png`, 그레이는 `walk.png`를 첫 번째 걷기 종류의 기준 자세로 선택하고 각각 실제 6프레임 보행 사이클을 제작했다. 파일은 `{appearance}/walk-01/frame-01.png`부터 `frame-06.png`까지 저장한다. `HamsterSprite`는 해당 프레임 디렉터리가 있을 때만 200ms 간격으로 재생하며, 정지 시안끼리는 교차 재생하지 않는다. 케이지 내 목표 가구까지의 위치 이동은 약 850ms로 맞춘다.

## 2026-08-02 - 케이지 기본 가구 이미지 교체

기존 이모지 가구를 프로젝트 전용 픽셀 아트 PNG로 교체했다. `client/src/assets/cage-items/`에 `water-bottle.png`, `house.png`, `food-bowl.png`, `wheel.png`를 저장한다. 쳇바퀴는 햄스터의 기존 `wheel.png` 행동 이미지에서 은색 원형 프레임, 크림색 발판, 차콜 지지대 구성을 유지하고 햄스터만 제거한 디자인이다.

초기 데이터 로직 이전에 만든 `tester` 계정은 가구가 0개였기 때문에 `serializeState()`가 기본 가구와 정원 구획을 idempotent하게 보충하도록 변경했다. 기존 계정 상태 조회 후 `FOOD_BOWL`, `WATER_BOTTLE`, `HOUSE` 3종 생성 확인.

## 2026-08-02 - 물 공급 가구 3종과 자율 걷기

물 공급 방식을 `WATER_BOTTLE`(벽걸이 기본 물병), `HANDHELD_WATER_BOTTLE`(두 손으로 잡는 젖병형), `WATER_BOWL`(낮은 물그릇) 3종으로 분리했다. 벽걸이형만 기본 제공하고 나머지 두 종류는 각각 50, 30 재화의 상점 가구다. 물 주기 행동은 보유한 세 종류 중 하나를 찾아 이동한다.

꾸미기 모드는 기존 가구 선택 후 위치 클릭 방식에 포인터 드래그를 추가했다. 케이지 화면이 열려 있고 행동 실행, 꾸미기, 상세 모달 상태가 아닐 때 햄스터는 0.5~1.4초 간격으로 다음 안전 좌표를 정하고 1.1초 동안 걷는다. 좌우 이동 방향에 맞춰 스프라이트를 반전한다.

자율 행동 선택 시 보유 중인 쳇바퀴와 집을 확인한다. 쳇바퀴는 약 20% 확률로 가구 좌표까지 이동해 2.6초 동안 걷기 프레임으로 달리는 모습을 보여준다. 집은 다음 20% 구간에서 입구까지 이동한 뒤 햄스터를 숨기고 3초간 `Z z z` 상태로 쉬었다가 집 옆으로 나온다. 행동 위치는 사용자가 꾸미기에서 저장한 최신 가구 좌표를 따른다.

쳇바퀴 가구 자체도 회전해 보이도록 `cage-items/wheel-spin/frame-01.png`부터 `frame-04.png`까지 제작했다. 받침, 지지대, 중심축은 고정하고 원형 림, 살, 발판의 회전 위상만 바꾼 프레임이며 햄스터가 쳇바퀴를 사용하는 2.6초 동안 160ms 간격으로 반복한다.

## 2026-08-17 - 당근 수확 애니메이션 단계별 실험

정원 수확을 즉시 API 호출하는 방식에서 햄스터가 대상 칸까지 이동한 뒤 작업하는 명령 구조로 연결했다. 개발 중인 최근 접속 계정(지민)의 16칸은 모두 `CARROT / READY`로 맞췄고, 위치 검수를 위해 정원 햄스터의 평상시 배회와 선택 칸 추적은 잠시 껐다.

잡초 제거 프레임 재사용은 폐기했다. 잡초와 당근이 한 화면에 겹치고 동작 의미도 맞지 않았다. `farmer-harvest-carrot-01`에 당근 전용 햄스터 포즈 6장을 마련했고, 원본 정원 아틀라스에서 흙 없는 성숙 당근 `carrot-harvested.png`도 추출했다. 수확 위치는 사용자가 화면에서 맞춘 `HARVEST_WORK_OFFSET = { x: -3, y: 0 }`이며, 원본 프레임을 반전해 햄스터가 당근 왼쪽에서 오른쪽을 바라보는 구도다.

프레임별로 `햄스터 몸 / 당근 / 앞팔 / 앞다리`를 DOM과 `clip-path`로 합성하는 방식을 1~3번 프레임에서 시험했다. 2번은 손을 가슴에 모은 잘못된 포즈라 손 사이에 당근 몸통 공간이 남는 이미지로 교체했다. 3번은 오른손과 앞다리를 별도 전경으로 꺼내는 작업까지 진행했다. 다만 이 방식은 햄스터가 `%`, 당근이 수확 시작 시 측정한 `px`, 팔다리가 별도 마스크 좌표를 사용해 창 최소화·최대화나 화면 배율 변경 시 서로 어긋난다. 손만 깔끔하게 분리하기도 어렵고 코드 복잡도도 계속 늘어난다는 것을 확인했다.

제품 구현 결정. 현재 다중 DOM 레이어는 실험 기록으로만 남기고, 다음 작업에서 각 프레임을 `햄스터 + 흙 없는 당근 + 전경 팔다리`가 이미 합쳐진 단일 PNG로 전환한다. 밭의 흙 또는 수확 구멍만 별도 레이어로 유지한다. 우선 3번 합성 PNG 한 장으로 PC·모바일·창 크기 변경 시 비율이 고정되는지 확인하고, 통과하면 1~6번 전체를 같은 512×512 기준점으로 만든 뒤 프레임 재생과 실제 수확 API를 다시 연결한다. 현재 `HARVEST_ALIGNMENT_PREVIEW = true`라 3번 정지 검수 화면에서 멈추며 실제 수확은 실행되지 않는다.

에셋 추출 재현용 PowerShell 스크립트는 `scripts/extract-harvest-frames.ps1`, `extract-single-chroma-frame.ps1`, `extract-mature-carrot.ps1`, `extract-frame-layer.ps1`에 남겼다. 이미지 생성 결과의 체크무늬/초록 배경 제거와 원본 아틀라스 당근 추출에 사용한다.
